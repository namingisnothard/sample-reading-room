'use strict';
(()=>{
 const data={"commit": "63984fda14bc66900c262414c256d0e8c9e21752", "repo": "https://github.com/physercoe/egoengine-repro", "cards": [{"id": "S3.SS1.p1.1", "title": "Reconstruct the digital twin", "inputs": "RGB frames, camera geometry, hand poses, object mesh", "outputs": "Aligned scene geometry, masks and object trajectory", "pseudo": "depth ← FoundationStereo(RGB)\nhuman_mask ← SAM2(RGB, hand_keypoint_prompts)\nobject_mask ← SAM2.track(RGB, first_frame_object_prompt)\nT_object[t] ← FoundationPose(RGB[t], depth[t], object_mask[t], mesh)\ntwin ← align(camera, depth, masks, hand_poses, mesh, T_object)", "explanation": "Recover per-frame absolute depth from the captured images. Use hand keypoints to prompt arm/hand segmentation. Propagate the task-object mask through the clip. Estimate the object’s 6D pose over time using RGB-D and its mesh. Put geometry and motion into the shared coordinates consumed by both branches.", "note": "Paper-level reconstruction. The reproduction loads supplied TACO calibration and poses; it does not implement the full FoundationStereo → FoundationPose path.", "implementations": [{"symbol": "load_sequence()", "path": "src/egoengine/taco/loader.py", "start": 225, "end": 249, "operation": "Load synchronized camera calibration, hand poses, object poses and RGB metadata into TacoSequence.", "note": "Partial counterpart: uses supplied TACO poses; this is not the paper’s FoundationStereo → FoundationPose reconstruction pipeline.", "key": "src/egoengine/taco/loader.py:225", "contract": "Inputs: a TACO task triplet and sequence ID. Output: one TacoSequence containing aligned video metadata, camera calibration, hand data and object trajectories.", "docstring": "Load one TACO egocentric sequence into a unified per-frame structure.", "example": "A cut/knife/plate sequence gives downstream stages one shared frame index: frame t selects the RGB image, camera pose, hands and both objects.", "steps": [{"start": 229, "end": 232, "code": "    cam_dir = join(root, \"Egocentric_Camera_Parameters\", triplet, seq_id)\n    hand_dir = join(root, \"Hand_Poses\", triplet, seq_id)\n    pose_dir = join(root, \"Object_Poses\", triplet, seq_id)\n    video_path = join(root, \"Egocentric_RGB_Videos\", triplet, seq_id, \"color.mp4\")", "explanation": "Construct the four dataset locations. The triplet and sequence ID identify the same demonstration in every modality."}, {"start": 233, "end": 235, "code": "    for p in (cam_dir, hand_dir, pose_dir, video_path):\n        if not (isdir(p) or isfile(p)):\n            raise FileNotFoundError(p)", "explanation": "Fail immediately if a required modality is absent, instead of silently returning an incomplete digital twin."}, {"start": 237, "end": 238, "code": "    intrinsic = np.loadtxt(join(cam_dir, \"egocentric_intrinsic.txt\")).astype(np.float32)\n    extrinsics = np.load(join(cam_dir, \"egocentric_frame_extrinsic.npy\")).astype(np.float32)", "explanation": "Read camera intrinsics and per-frame extrinsics. These are supplied calibration values, not estimates produced by a depth network."}, {"start": 239, "end": 241, "code": "    hands = {side: _load_hand(hand_dir, side) for side in (\"left\", \"right\")}\n    _, tool_name, target_name = parse_triplet(triplet)\n    objects = _load_objects(pose_dir, model_root, {\"tool\": tool_name, \"target\": target_name})", "explanation": "Load both hands and resolve tool/target names into object meshes and pose trajectories. The loader consumes annotations; it does not run FoundationPose."}, {"start": 242, "end": 249, "code": "    n_frames, fps, width, height = _probe_video(video_path)\n\n    return TacoSequence(\n        triplet=triplet, seq_id=seq_id, root=root,\n        n_frames=n_frames, fps=fps, width=width, height=height,\n        video_path=video_path, intrinsic=intrinsic, extrinsics=extrinsics,\n        hands=hands, objects=objects,\n    )", "explanation": "Read video timing and image dimensions, then package all modalities under one sequence object for later projection, retargeting and rendering."}]}, {"symbol": "hand_prompt_points() / segment_hands()", "path": "src/egoengine/visual/mask.py", "start": 33, "end": 95, "operation": "Project MANO keypoints into the image, prompt SAM2 on the first frame, and propagate the union of hand masks through the clip.", "note": "Implements hand masks; not the paper’s separate task-object tracking branch.", "key": "src/egoengine/visual/mask.py:33", "contract": "Inputs: sequence camera geometry, selected frame IDs and MANO hand joints. Output: a Boolean human-hand mask per requested frame.", "docstring": "Project each hand's MANO joints into `frame_idx`; keep in-image points.\n\nReturns {side: (points (M,2) float32 xy, labels (M,) int32 all-ones)}.\nSides with < 3 valid projected joints are dropped (hand not visible).", "example": "Two tracked hands become one removal mask via any(dim=0). The robot visibility mask is computed later by rendering, not by SAM2.", "steps": [{"start": 39, "end": 43, "code": "    prompts = {}\n    for side, joints in joints_by_side.items():\n        uv, z = seq.project(joints[PROMPT_JOINTS], frame_idx)\n        ok = (z > 0) & (uv[:, 0] >= 0) & (uv[:, 0] < seq.width) & \\\n             (uv[:, 1] >= 0) & (uv[:, 1] < seq.height)", "explanation": "Project selected 3D hand joints into image coordinates; reject points behind the camera or outside the frame."}, {"start": 44, "end": 46, "code": "        pts = uv[ok].astype(np.float32)\n        if pts.shape[0] >= 3:\n            prompts[side] = (pts, np.ones(pts.shape[0], dtype=np.int32))", "explanation": "Use surviving points as positive SAM2 prompts, but only if at least three are visible for that hand."}, {"start": 60, "end": 70, "code": "    # MANO joints at the prompt frame (first frame of the clip)\n    f0 = frames[0]\n    joints_by_side = {}\n    for side, hand in seq.hands.items():\n        h1 = dataclasses.replace(hand, pose=hand.pose[f0:f0 + 1],\n                                 trans=hand.trans[f0:f0 + 1],\n                                 frame_ids=hand.frame_ids[f0:f0 + 1])\n        _, joints, _ = mano_forward(h1)\n        joints_by_side[side] = joints[0]\n\n    prompts = hand_prompt_points(seq, f0, joints_by_side)", "explanation": "Reconstruct MANO joints at the clip’s first frame and derive image-space hand prompts from them."}, {"start": 71, "end": 72, "code": "    if not prompts:\n        raise RuntimeError(f\"no visible hand joints at frame {f0} — cannot prompt SAM2\")", "explanation": "Reject clips with no visible prompts instead of inventing a hand mask."}, {"start": 78, "end": 87, "code": "    tmp = tempfile.mkdtemp(prefix=\"sam2_frames_\")\n    for i, f in enumerate(frames):\n        img = seq.read_frame(f)\n        cv2.imwrite(str(Path(tmp) / f\"{i:06d}.jpg\"), img[:, :, ::-1])\n\n    predictor = build_sam2_video_predictor(CFG, str(CKPT), device=device)\n    masks: dict[int, np.ndarray] = {}\n    with torch.inference_mode(), torch.autocast(\"cuda\", dtype=torch.bfloat16):\n        state = predictor.init_state(video_path=tmp, offload_video_to_cpu=True,\n                                     offload_state_to_cpu=True)", "explanation": "Extract the selected RGB frames and initialize the SAM2 video predictor and tracking state."}, {"start": 89, "end": 91, "code": "        for obj_id, (side, (pts, labels)) in enumerate(prompts.items(), start=1):\n            predictor.add_new_points_or_box(state, frame_idx=0, obj_id=obj_id,\n                                            points=pts, labels=labels)", "explanation": "Assign a distinct object ID to each hand and register its positive point prompts on the first clip frame."}, {"start": 92, "end": 95, "code": "        for out_f, out_obj_ids, out_logits in predictor.propagate_in_video(state):\n            m = (out_logits > 0.0).any(dim=0).cpu().numpy().astype(bool)\n            m = np.squeeze(m)  # (n_obj,1,H,W) -> any over obj -> (1,H,W) -> (H,W)\n            masks[frames[out_f]] = m", "explanation": "Propagate through the video, threshold logits at zero, union all tracked hands, and map clip-relative indices back to the requested source frame IDs."}]}]}, {"id": "A1.SS3.p1.1", "title": "Reconstruct the digital twin", "inputs": "RGB frames, camera geometry, hand poses, object mesh", "outputs": "Aligned scene geometry, masks and object trajectory", "pseudo": "depth ← FoundationStereo(RGB)\nhuman_mask ← SAM2(RGB, hand_keypoint_prompts)\nobject_mask ← SAM2.track(RGB, first_frame_object_prompt)\nT_object[t] ← FoundationPose(RGB[t], depth[t], object_mask[t], mesh)\ntwin ← align(camera, depth, masks, hand_poses, mesh, T_object)", "explanation": "Recover per-frame absolute depth from the captured images. Use hand keypoints to prompt arm/hand segmentation. Propagate the task-object mask through the clip. Estimate the object’s 6D pose over time using RGB-D and its mesh. Put geometry and motion into the shared coordinates consumed by both branches.", "note": "Paper-level reconstruction. The reproduction loads supplied TACO calibration and poses; it does not implement the full FoundationStereo → FoundationPose path.", "implementations": [{"symbol": "load_sequence()", "path": "src/egoengine/taco/loader.py", "start": 225, "end": 249, "operation": "Load synchronized camera calibration, hand poses, object poses and RGB metadata into TacoSequence.", "note": "Partial counterpart: uses supplied TACO poses; this is not the paper’s FoundationStereo → FoundationPose reconstruction pipeline.", "key": "src/egoengine/taco/loader.py:225", "contract": "Inputs: a TACO task triplet and sequence ID. Output: one TacoSequence containing aligned video metadata, camera calibration, hand data and object trajectories.", "docstring": "Load one TACO egocentric sequence into a unified per-frame structure.", "example": "A cut/knife/plate sequence gives downstream stages one shared frame index: frame t selects the RGB image, camera pose, hands and both objects.", "steps": [{"start": 229, "end": 232, "code": "    cam_dir = join(root, \"Egocentric_Camera_Parameters\", triplet, seq_id)\n    hand_dir = join(root, \"Hand_Poses\", triplet, seq_id)\n    pose_dir = join(root, \"Object_Poses\", triplet, seq_id)\n    video_path = join(root, \"Egocentric_RGB_Videos\", triplet, seq_id, \"color.mp4\")", "explanation": "Construct the four dataset locations. The triplet and sequence ID identify the same demonstration in every modality."}, {"start": 233, "end": 235, "code": "    for p in (cam_dir, hand_dir, pose_dir, video_path):\n        if not (isdir(p) or isfile(p)):\n            raise FileNotFoundError(p)", "explanation": "Fail immediately if a required modality is absent, instead of silently returning an incomplete digital twin."}, {"start": 237, "end": 238, "code": "    intrinsic = np.loadtxt(join(cam_dir, \"egocentric_intrinsic.txt\")).astype(np.float32)\n    extrinsics = np.load(join(cam_dir, \"egocentric_frame_extrinsic.npy\")).astype(np.float32)", "explanation": "Read camera intrinsics and per-frame extrinsics. These are supplied calibration values, not estimates produced by a depth network."}, {"start": 239, "end": 241, "code": "    hands = {side: _load_hand(hand_dir, side) for side in (\"left\", \"right\")}\n    _, tool_name, target_name = parse_triplet(triplet)\n    objects = _load_objects(pose_dir, model_root, {\"tool\": tool_name, \"target\": target_name})", "explanation": "Load both hands and resolve tool/target names into object meshes and pose trajectories. The loader consumes annotations; it does not run FoundationPose."}, {"start": 242, "end": 249, "code": "    n_frames, fps, width, height = _probe_video(video_path)\n\n    return TacoSequence(\n        triplet=triplet, seq_id=seq_id, root=root,\n        n_frames=n_frames, fps=fps, width=width, height=height,\n        video_path=video_path, intrinsic=intrinsic, extrinsics=extrinsics,\n        hands=hands, objects=objects,\n    )", "explanation": "Read video timing and image dimensions, then package all modalities under one sequence object for later projection, retargeting and rendering."}]}]}, {"id": "A1.SS3.p3.1", "title": "Reconstruct the digital twin", "inputs": "RGB frames, camera geometry, hand poses, object mesh", "outputs": "Aligned scene geometry, masks and object trajectory", "pseudo": "depth ← FoundationStereo(RGB)\nhuman_mask ← SAM2(RGB, hand_keypoint_prompts)\nobject_mask ← SAM2.track(RGB, first_frame_object_prompt)\nT_object[t] ← FoundationPose(RGB[t], depth[t], object_mask[t], mesh)\ntwin ← align(camera, depth, masks, hand_poses, mesh, T_object)", "explanation": "Recover per-frame absolute depth from the captured images. Use hand keypoints to prompt arm/hand segmentation. Propagate the task-object mask through the clip. Estimate the object’s 6D pose over time using RGB-D and its mesh. Put geometry and motion into the shared coordinates consumed by both branches.", "note": "Paper-level reconstruction. The reproduction loads supplied TACO calibration and poses; it does not implement the full FoundationStereo → FoundationPose path.", "implementations": [{"symbol": "load_sequence()", "path": "src/egoengine/taco/loader.py", "start": 225, "end": 249, "operation": "Load synchronized camera calibration, hand poses, object poses and RGB metadata into TacoSequence.", "note": "Partial counterpart: uses supplied TACO poses; this is not the paper’s FoundationStereo → FoundationPose reconstruction pipeline.", "key": "src/egoengine/taco/loader.py:225", "contract": "Inputs: a TACO task triplet and sequence ID. Output: one TacoSequence containing aligned video metadata, camera calibration, hand data and object trajectories.", "docstring": "Load one TACO egocentric sequence into a unified per-frame structure.", "example": "A cut/knife/plate sequence gives downstream stages one shared frame index: frame t selects the RGB image, camera pose, hands and both objects.", "steps": [{"start": 229, "end": 232, "code": "    cam_dir = join(root, \"Egocentric_Camera_Parameters\", triplet, seq_id)\n    hand_dir = join(root, \"Hand_Poses\", triplet, seq_id)\n    pose_dir = join(root, \"Object_Poses\", triplet, seq_id)\n    video_path = join(root, \"Egocentric_RGB_Videos\", triplet, seq_id, \"color.mp4\")", "explanation": "Construct the four dataset locations. The triplet and sequence ID identify the same demonstration in every modality."}, {"start": 233, "end": 235, "code": "    for p in (cam_dir, hand_dir, pose_dir, video_path):\n        if not (isdir(p) or isfile(p)):\n            raise FileNotFoundError(p)", "explanation": "Fail immediately if a required modality is absent, instead of silently returning an incomplete digital twin."}, {"start": 237, "end": 238, "code": "    intrinsic = np.loadtxt(join(cam_dir, \"egocentric_intrinsic.txt\")).astype(np.float32)\n    extrinsics = np.load(join(cam_dir, \"egocentric_frame_extrinsic.npy\")).astype(np.float32)", "explanation": "Read camera intrinsics and per-frame extrinsics. These are supplied calibration values, not estimates produced by a depth network."}, {"start": 239, "end": 241, "code": "    hands = {side: _load_hand(hand_dir, side) for side in (\"left\", \"right\")}\n    _, tool_name, target_name = parse_triplet(triplet)\n    objects = _load_objects(pose_dir, model_root, {\"tool\": tool_name, \"target\": target_name})", "explanation": "Load both hands and resolve tool/target names into object meshes and pose trajectories. The loader consumes annotations; it does not run FoundationPose."}, {"start": 242, "end": 249, "code": "    n_frames, fps, width, height = _probe_video(video_path)\n\n    return TacoSequence(\n        triplet=triplet, seq_id=seq_id, root=root,\n        n_frames=n_frames, fps=fps, width=width, height=height,\n        video_path=video_path, intrinsic=intrinsic, extrinsics=extrinsics,\n        hands=hands, objects=objects,\n    )", "explanation": "Read video timing and image dimensions, then package all modalities under one sequence object for later projection, retargeting and rendering."}]}]}, {"id": "A1.SS1.p1.1", "title": "Place a pseudo robot base for TACO", "inputs": "Tool and target positions; fixed offset; table height", "outputs": "A consistent scene-to-robot alignment", "pseudo": "center ← midpoint(tool_position, target_position)\nbase_position ← center + fixed_offset\nset_table_height(h_table)\nT_base ← construct_consistent_base_frame(base_position)\nalign(hand_trajectory, object_trajectory, T_base)", "explanation": "Use the two task objects to anchor the robot location. Place the robot at the prescribed offset from the scene center; the coordinate convention determines its sign. Match the table geometry used by the robot setup. Instantiate a consistent robot frame across episodes. Express motion in the same coordinates as the simulator robot.", "note": "TACO only. Appendix A uses AprilTags for the Aria setup. Camera-direction averaging and the exact basis construction are reproduction choices.", "implementations": [{"symbol": "compute_pseudo_base()", "path": "src/egoengine/taco/pseudo_base.py", "start": 30, "end": 63, "operation": "Average the tool/target midpoint, apply a horizontal offset and table height, then assemble the base-to-world transform.", "note": "TACO heuristic only; no Aria AprilTag calibration.", "key": "src/egoengine/taco/pseudo_base.py:30", "contract": "Input: a sequence with tool/target poses and camera positions. Output: a 4×4 base-to-world rigid transform; defaults are 0.6 m offset and 0.72 m table height.", "docstring": "Robot base frame (4x4, base-to-world) for a TacoSequence.\n\nArgs:\n    sequence: TacoSequence with tool + target objects loaded.\n    offset: horizontal distance (m) from scene center to the base.\n    table_height: table surface height above the base frame (m).", "example": "If c=(0,0,0.72) and d=(0,1,0), the default origin is (0,−0.6,0). This heuristic replaces calibration only for the TACO path.", "steps": [{"start": 38, "end": 41, "code": "    tool_t = sequence.objects[\"tool\"].poses[:, :3, 3]\n    target_t = sequence.objects[\"target\"].poses[:, :3, 3]\n    center = 0.5 * (tool_t + target_t)\n    c = center.mean(axis=0)", "explanation": "Extract object translations, average tool and target at each frame, then average across time to obtain a stable scene center c."}, {"start": 43, "end": 44, "code": "    cam_centers = np.stack([sequence.camera_position(i) for i in range(sequence.n_frames)])\n    cam_mean = cam_centers.mean(axis=0)", "explanation": "Average the camera centers over the sequence to estimate the viewing side of the workspace."}, {"start": 46, "end": 49, "code": "    d = cam_mean - c\n    d[2] = 0.0\n    norm = np.linalg.norm(d)\n    d = d / norm if norm > 1e-6 else np.array([0.0, -1.0, 0.0])", "explanation": "Compute the horizontal direction from scene center toward camera. Set z to zero and normalize; use a fixed direction if its length is almost zero."}, {"start": 51, "end": 51, "code": "    origin = np.array([c[0] - offset * d[0], c[1] - offset * d[1], c[2] - table_height])", "explanation": "Place the base opposite the camera-facing direction by offset, and below the center by table_height. The sign of the subtraction determines which side receives the robot."}, {"start": 53, "end": 56, "code": "    x_axis = -d  # base X points toward the scene center\n    z_axis = np.array([0.0, 0.0, 1.0])\n    y_axis = np.cross(z_axis, x_axis)\n    y_axis /= np.linalg.norm(y_axis)", "explanation": "Build an upright basis: z is vertical, x is −d, and y is their cross product. These three vectors define the base orientation."}, {"start": 58, "end": 63, "code": "    T = np.eye(4, dtype=np.float64)\n    T[:3, 0] = x_axis\n    T[:3, 1] = y_axis\n    T[:3, 2] = z_axis\n    T[:3, 3] = origin\n    return T", "explanation": "Write the basis vectors into the rotation columns and origin into the translation column of a homogeneous transform."}]}]}, {"id": "A1.SS1.p6.1", "title": "Place a pseudo robot base for TACO", "inputs": "Tool and target positions; fixed offset; table height", "outputs": "A consistent scene-to-robot alignment", "pseudo": "center ← midpoint(tool_position, target_position)\nbase_position ← center + fixed_offset\nset_table_height(h_table)\nT_base ← construct_consistent_base_frame(base_position)\nalign(hand_trajectory, object_trajectory, T_base)", "explanation": "Use the two task objects to anchor the robot location. Place the robot at the prescribed offset from the scene center; the coordinate convention determines its sign. Match the table geometry used by the robot setup. Instantiate a consistent robot frame across episodes. Express motion in the same coordinates as the simulator robot.", "note": "TACO only. Appendix A uses AprilTags for the Aria setup. Camera-direction averaging and the exact basis construction are reproduction choices.", "implementations": [{"symbol": "compute_pseudo_base()", "path": "src/egoengine/taco/pseudo_base.py", "start": 30, "end": 63, "operation": "Average the tool/target midpoint, apply a horizontal offset and table height, then assemble the base-to-world transform.", "note": "TACO heuristic only; no Aria AprilTag calibration.", "key": "src/egoengine/taco/pseudo_base.py:30", "contract": "Input: a sequence with tool/target poses and camera positions. Output: a 4×4 base-to-world rigid transform; defaults are 0.6 m offset and 0.72 m table height.", "docstring": "Robot base frame (4x4, base-to-world) for a TacoSequence.\n\nArgs:\n    sequence: TacoSequence with tool + target objects loaded.\n    offset: horizontal distance (m) from scene center to the base.\n    table_height: table surface height above the base frame (m).", "example": "If c=(0,0,0.72) and d=(0,1,0), the default origin is (0,−0.6,0). This heuristic replaces calibration only for the TACO path.", "steps": [{"start": 38, "end": 41, "code": "    tool_t = sequence.objects[\"tool\"].poses[:, :3, 3]\n    target_t = sequence.objects[\"target\"].poses[:, :3, 3]\n    center = 0.5 * (tool_t + target_t)\n    c = center.mean(axis=0)", "explanation": "Extract object translations, average tool and target at each frame, then average across time to obtain a stable scene center c."}, {"start": 43, "end": 44, "code": "    cam_centers = np.stack([sequence.camera_position(i) for i in range(sequence.n_frames)])\n    cam_mean = cam_centers.mean(axis=0)", "explanation": "Average the camera centers over the sequence to estimate the viewing side of the workspace."}, {"start": 46, "end": 49, "code": "    d = cam_mean - c\n    d[2] = 0.0\n    norm = np.linalg.norm(d)\n    d = d / norm if norm > 1e-6 else np.array([0.0, -1.0, 0.0])", "explanation": "Compute the horizontal direction from scene center toward camera. Set z to zero and normalize; use a fixed direction if its length is almost zero."}, {"start": 51, "end": 51, "code": "    origin = np.array([c[0] - offset * d[0], c[1] - offset * d[1], c[2] - table_height])", "explanation": "Place the base opposite the camera-facing direction by offset, and below the center by table_height. The sign of the subtraction determines which side receives the robot."}, {"start": 53, "end": 56, "code": "    x_axis = -d  # base X points toward the scene center\n    z_axis = np.array([0.0, 0.0, 1.0])\n    y_axis = np.cross(z_axis, x_axis)\n    y_axis /= np.linalg.norm(y_axis)", "explanation": "Build an upright basis: z is vertical, x is −d, and y is their cross product. These three vectors define the base orientation."}, {"start": 58, "end": 63, "code": "    T = np.eye(4, dtype=np.float64)\n    T[:3, 0] = x_axis\n    T[:3, 1] = y_axis\n    T[:3, 2] = z_axis\n    T[:3, 3] = origin\n    return T", "explanation": "Write the basis vectors into the rotation columns and origin into the translation column of a homogeneous transform."}]}]}, {"id": "A1.SS3.p2.1", "title": "Place a pseudo robot base for TACO", "inputs": "Tool and target positions; fixed offset; table height", "outputs": "A consistent scene-to-robot alignment", "pseudo": "center ← midpoint(tool_position, target_position)\nbase_position ← center + fixed_offset\nset_table_height(h_table)\nT_base ← construct_consistent_base_frame(base_position)\nalign(hand_trajectory, object_trajectory, T_base)", "explanation": "Use the two task objects to anchor the robot location. Place the robot at the prescribed offset from the scene center; the coordinate convention determines its sign. Match the table geometry used by the robot setup. Instantiate a consistent robot frame across episodes. Express motion in the same coordinates as the simulator robot.", "note": "TACO only. Appendix A uses AprilTags for the Aria setup. Camera-direction averaging and the exact basis construction are reproduction choices.", "implementations": [{"symbol": "compute_pseudo_base()", "path": "src/egoengine/taco/pseudo_base.py", "start": 30, "end": 63, "operation": "Average the tool/target midpoint, apply a horizontal offset and table height, then assemble the base-to-world transform.", "note": "TACO heuristic only; no Aria AprilTag calibration.", "key": "src/egoengine/taco/pseudo_base.py:30", "contract": "Input: a sequence with tool/target poses and camera positions. Output: a 4×4 base-to-world rigid transform; defaults are 0.6 m offset and 0.72 m table height.", "docstring": "Robot base frame (4x4, base-to-world) for a TacoSequence.\n\nArgs:\n    sequence: TacoSequence with tool + target objects loaded.\n    offset: horizontal distance (m) from scene center to the base.\n    table_height: table surface height above the base frame (m).", "example": "If c=(0,0,0.72) and d=(0,1,0), the default origin is (0,−0.6,0). This heuristic replaces calibration only for the TACO path.", "steps": [{"start": 38, "end": 41, "code": "    tool_t = sequence.objects[\"tool\"].poses[:, :3, 3]\n    target_t = sequence.objects[\"target\"].poses[:, :3, 3]\n    center = 0.5 * (tool_t + target_t)\n    c = center.mean(axis=0)", "explanation": "Extract object translations, average tool and target at each frame, then average across time to obtain a stable scene center c."}, {"start": 43, "end": 44, "code": "    cam_centers = np.stack([sequence.camera_position(i) for i in range(sequence.n_frames)])\n    cam_mean = cam_centers.mean(axis=0)", "explanation": "Average the camera centers over the sequence to estimate the viewing side of the workspace."}, {"start": 46, "end": 49, "code": "    d = cam_mean - c\n    d[2] = 0.0\n    norm = np.linalg.norm(d)\n    d = d / norm if norm > 1e-6 else np.array([0.0, -1.0, 0.0])", "explanation": "Compute the horizontal direction from scene center toward camera. Set z to zero and normalize; use a fixed direction if its length is almost zero."}, {"start": 51, "end": 51, "code": "    origin = np.array([c[0] - offset * d[0], c[1] - offset * d[1], c[2] - table_height])", "explanation": "Place the base opposite the camera-facing direction by offset, and below the center by table_height. The sign of the subtraction determines which side receives the robot."}, {"start": 53, "end": 56, "code": "    x_axis = -d  # base X points toward the scene center\n    z_axis = np.array([0.0, 0.0, 1.0])\n    y_axis = np.cross(z_axis, x_axis)\n    y_axis /= np.linalg.norm(y_axis)", "explanation": "Build an upright basis: z is vertical, x is −d, and y is their cross product. These three vectors define the base orientation."}, {"start": 58, "end": 63, "code": "    T = np.eye(4, dtype=np.float64)\n    T[:3, 0] = x_axis\n    T[:3, 1] = y_axis\n    T[:3, 2] = z_axis\n    T[:3, 3] = origin\n    return T", "explanation": "Write the basis vectors into the rotation columns and origin into the translation column of a homogeneous transform."}]}]}, {"id": "S3.SS2.SSS1.p1.1", "title": "Retarget human motion with inverse kinematics", "inputs": "Human fingertip poses and wrist orientation at each frame", "outputs": "Reference robot configuration trajectory q_ref", "pseudo": "for each frame t:\n    targets ← human_fingertip_poses[t], human_wrist_orientation[t]\n    L(q) ← fingertip_pose_loss(q, targets) + wrist_orientation_loss(q, targets)\n    Q ← joint_limits ∩ self_collision_constraints\n    q_ref[t] ← MINK.solve(L, q ∈ Q)\nreturn q_ref", "explanation": "Solve the pose-matching problem for each instant of the demonstration. Extract the human motion features the robot should match. Measure robot/human fingertip and wrist discrepancies, using the paper’s weighting. The paper’s feasible set includes joint limits and self-collision constraints. Find a feasible robot configuration minimizing the pose discrepancy. Use the resulting kinematic trajectory as the input to physical refinement.", "note": "The reproduction configures joint limits but no self-collision constraint, and uses Inspire hands instead of XHand.", "implementations": [{"symbol": "HandRetargeter.retarget()", "path": "src/egoengine/retarget/ik.py", "start": 230, "end": 267, "operation": "Set fingertip/wrist targets → solve mink IK → integrate joint velocity; warm-start each frame from the previous solution.", "note": "Uses Inspire RH56 hands. Configuration limits are enabled; the paper’s self-collision constraint is not configured here.", "key": "src/egoengine/retarget/ik.py:230", "contract": "Input: N frames of 21 hand joints, shaped [N,21,3], in the pseudo-base frame. Output: robot configurations and fingertip/wrist residuals for each frame.", "docstring": "IK over a MANO joint trajectory (N,21,3) in the pseudo-base frame.\n\nWarm-started: the solution of frame t seeds frame t+1.", "example": "Matching a fingertip pose is not evidence of a stable grasp: this solve does not simulate contact forces. That is why the paper adds action refinement.", "steps": [{"start": 236, "end": 240, "code": "        n = joints_traj.shape[0]\n        qpos = np.zeros((n, self.model.nq))\n        tip_err = np.zeros((n, 5))\n        wrist_err = np.zeros(n)\n        tip_tgt = np.zeros((n, 5, 3))", "explanation": "Allocate one robot configuration per frame plus arrays for diagnostic errors and targets."}, {"start": 242, "end": 246, "code": "        for t in range(n):\n            tgt, w = self._targets(joints_traj[t])\n            for f in FINGER_NAMES:\n                self.tip_tasks[f].set_target(mink.SE3.from_matrix(tgt[f]))\n            self.wrist_task.set_target(mink.SE3.from_matrix(w))", "explanation": "Convert the current human joints into fingertip/wrist transforms, then set the targets of the existing MINK tasks."}, {"start": 247, "end": 250, "code": "            for _ in range(iters_first if t == 0 else iters):\n                vel = mink.solve_ik(\n                    self.config, self.tasks, dt=dt, solver=\"daqp\",\n                    limits=self.limits, damping=1e-4,", "explanation": "Solve a constrained velocity-level IK problem. The first frame gets more iterations; later frames start from the previous configuration. DAQP is the selected QP solver."}, {"start": 251, "end": 253, "code": "                )\n                self.config.integrate_inplace(vel, dt)\n            qpos[t] = self.config.q", "explanation": "Integrate the solved velocity for dt and store the resulting configuration. No reset inside the frame loop means the previous solution is the next warm start."}, {"start": 254, "end": 258, "code": "            tip_tgt[t] = [tgt[f][:3, 3] for f in FINGER_NAMES]\n            # residuals via FK at the solved configuration\n            for i, f in enumerate(FINGER_NAMES):\n                cur = self.config.get_transform_frame_to_world(f\"{f}_tip\", \"site\")\n                tip_err[t, i] = np.linalg.norm(cur.translation() - tgt[f][:3, 3]) * 100.0", "explanation": "Use forward kinematics at the solved configuration to measure fingertip distance from target; multiply meters by 100 to report centimeters."}, {"start": 259, "end": 262, "code": "            r_cur = self.config.get_transform_frame_to_world(PALM_LINK, \"body\").rotation().as_matrix()\n            r_d = w[:3, :3].T @ r_cur\n            cos = np.clip((np.trace(r_d) - 1.0) / 2.0, -1.0, 1.0)\n            wrist_err[t] = np.degrees(np.arccos(cos))", "explanation": "Compare desired and achieved wrist rotations. The trace formula produces an angular error, clamped before acos to avoid numerical domain errors."}, {"start": 264, "end": 267, "code": "        return RetargetResult(\n            side=self.side, qpos=qpos, tip_err_cm=tip_err,\n            wrist_ori_err_deg=wrist_err, tip_target=tip_tgt,\n            scale=self.scale, joint_names=self.joint_names,", "explanation": "Return both the trajectory and its error report, so kinematic matching can be inspected separately from physical execution."}]}, {"symbol": "HandRetargeter.__init__()", "path": "src/egoengine/retarget/ik.py", "start": 195, "end": 210, "operation": "Build weighted fingertip and wrist FrameTasks and a ConfigurationLimit for the IK objective.", "note": "", "key": "src/egoengine/retarget/ik.py:195", "contract": "Setup for IK: one FrameTask per fingertip plus a wrist FrameTask. Costs weight position/orientation errors; ConfigurationLimit restricts joint configurations.", "docstring": "calib_joints21: MANO joints (21,3) of the FLAT reference hand\n(global_orient=0, hand_pose=0, subject betas) in any frame with the\nsame axis convention as the data (only relative geometry is used).", "example": "Increasing a fingertip position cost prioritizes its position residual relative to other task errors; it does not add a new physical contact constraint.", "steps": [{"start": 196, "end": 203, "code": "        self.tip_tasks = {\n            f: mink.FrameTask(\n                frame_name=f\"{f}_tip\", frame_type=\"site\",\n                position_cost=pos_cost, orientation_cost=ori_cost,\n                lm_damping=LM_DAMPING, gain=GAIN,\n            )\n            for f in FINGER_NAMES\n        }", "explanation": "Create a task at each fingertip site. position_cost and orientation_cost decide how strongly the optimizer tries to match translation and rotation."}, {"start": 204, "end": 208, "code": "        self.wrist_task = mink.FrameTask(\n            frame_name=PALM_LINK, frame_type=\"body\",\n            position_cost=pos_cost_wrist, orientation_cost=lambda_wrist,\n            lm_damping=LM_DAMPING, gain=GAIN,\n        )", "explanation": "Create a wrist task on the palm body. It includes a position cost as well as the wrist orientation weight."}, {"start": 209, "end": 210, "code": "        self.tasks = list(self.tip_tasks.values()) + [self.wrist_task]\n        self.limits = [mink.ConfigurationLimit(self.model)]", "explanation": "Collect tasks for the IK solve and install joint configuration limits. There is no CollisionAvoidanceLimit in this list."}]}]}, {"id": "S3.SS2.SSS2.p1.1", "title": "Evaluate reference motion in simulation", "inputs": "Reference robot trajectory and demonstrated object trajectory", "outputs": "Executed object motion and its tracking error", "pseudo": "initialize simulator at the starting scene state\nfor each control step t:\n    command ← reference_command(q_ref[t])\n    state ← simulate(state, command)\n    error[t] ← object_pose_error(state.object, T_object[t])", "explanation": "Place the robot and objects in the aligned starting configuration. Evaluate the trajectory over time. Convert the retargeted motion into the controller’s action representation. Advance dynamics so contact and embodiment mismatch affect the result. Judge the robot by the demonstrated object motion, not just hand-pose matching.", "note": "", "implementations": [{"symbol": "rollout()", "path": "scripts/run_refine.py", "start": 124, "end": 149, "operation": "Apply reference wrist/finger targets plus optional deltas, step physics, and measure the resulting object poses.", "note": "", "key": "scripts/run_refine.py:124", "contract": "Rollout loop: N parallel simulator environments over a frame window. Optional corrections have shapes [N,H,3] for wrist translation and [N,H,12] for fingers.", "docstring": "Roll all envs from frame t0 to t1 (exclusive). act_seqs: optional\n{side: (wrist_dxyz (N,H,3)T, q12_d (N,H,12)T)} deltas on the reference.\nReturns dict with per-frame per-env dp/dR (N,H) per role, contact flags,\nand env-0 executed/actual states if record.", "example": "With act_seqs=None this is Replay. Supplying sampled deltas reuses the same simulator loop to evaluate MPC candidates.", "steps": [{"start": 124, "end": 127, "code": "    prev = None\n    for k in range(hlen):\n        t = t0 + k\n        ra = ref_actions(prep, t)", "explanation": "Start a frame window with no previous command and retrieve the retargeted reference action at each frame."}, {"start": 129, "end": 133, "code": "        for s in SIDES:\n            p = torch.tensor(ra[s][0], dtype=torch.float32, device=dev)[None].expand(n, 3)\n            q = torch.tensor(ra[s][1], dtype=torch.float32, device=dev)[None].expand(n, 4)\n            v = torch.tensor(ra[s][2], dtype=torch.float32, device=dev)[None].expand(n, 3)\n            q12 = torch.tensor(ra[s][3], dtype=torch.float32, device=dev)[None].expand(n, 12)", "explanation": "Broadcast the reference wrist position, quaternion, velocity and 12 finger targets to all candidate environments."}, {"start": 134, "end": 137, "code": "            if act_seqs is not None:\n                p = p + act_seqs[s][0][:, k]\n                q12 = q12 + act_seqs[s][1][:, k]\n            acts[s] = (p, q, v, q12)", "explanation": "If candidate corrections exist, add them to wrist position and finger targets. This MPC path does not change the wrist quaternion."}, {"start": 138, "end": 142, "code": "        for sub in range(1, substeps + 1):\n            if prev is not None:\n                apply_action_frame(H, acts, prev, sub / substeps)\n            else:\n                apply_action_frame(H, acts, acts, 1.0)", "explanation": "Interpolate from the previous action over physics substeps; for the first frame, apply the current action directly."}, {"start": 143, "end": 145, "code": "            H[\"physx\"].simulate(H[\"dt\"], 0.0)\n            H[\"physx\"].fetch_results()\n        prev = acts", "explanation": "Advance PhysX and fetch results before reading state. These calls make the reference into a physical rollout rather than just a pose sequence."}, {"start": 146, "end": 149, "code": "        for r in roles:\n            ts = get_object_poses_batched(H, r)\n            dp[r][:, k], dR[r][:, k] = pose_error_batched(\n                ts, prep[\"objects\"][r][\"gt\"][t])", "explanation": "Read each simulated object pose and compare it with the demonstration at the same frame; store translation and rotation error for feasibility and scoring."}]}]}, {"id": "S3.SS2.SSS2.p2.1", "title": "Track the object and stop on failure", "inputs": "Simulated and demonstrated object poses; threshold C", "outputs": "Tracking reward while the trajectory remains valid", "pseudo": "d_position ← ‖p_sim − p_reference‖₂\nd_rotation ← geodesic_SO3(R_sim, R_reference)\nerror ← paper_tracking_error(d_position, d_rotation)\nif error > C: terminate rollout\notherwise: reward ← C − error", "explanation": "Compare simulated and demonstrated translations with Euclidean distance. Measure the shortest angular discrepancy between rotations. Use the object-tracking definition and weights of the surrounding paper equation. The paper describes early termination at the tracking boundary. Lower object error earns more reward within the valid regime.", "note": "The reproduction uses separate position/rotation thresholds. Its RL reward masking and rollout scorer also differ at the first violating frame; see Python below.", "implementations": [{"symbol": "pose_error_batched()", "path": "src/egoengine/sim/scene.py", "start": 611, "end": 622, "operation": "Compute Euclidean translation error and quaternion geodesic rotation error, 2·acos(|q·q_ref|).", "note": "", "key": "src/egoengine/sim/scene.py:611", "contract": "Input: N simulated poses [N,7] (xyz + quaternion xyzw) and one reference 4×4 transform. Output: dp in meters and dR in radians for each environment.", "docstring": "dp (N,), dR (N,) torch. t_sim (N,7) xyz+xyzw; gt_T (4,4) numpy.", "example": "Identical positions give dp=0. Identical rotations give |dot|=1 and dR=0; a 180° mismatch gives |dot|=0 and dR=π.", "steps": [{"start": 616, "end": 618, "code": "    p_gt = torch.tensor(gt_T[:3, 3], dtype=torch.float32, device=dev)\n    q_gt_xyzw = torch.tensor(Rotation.from_matrix(gt_T[:3, :3]).as_quat(),\n                             dtype=torch.float32, device=dev)", "explanation": "Convert the reference translation and rotation matrix to tensors on the simulation device, using xyzw quaternion order."}, {"start": 619, "end": 619, "code": "    dp = (t_sim[:, :3] - p_gt).norm(dim=-1)", "explanation": "Subtract reference position and take the vector norm: this is the Euclidean translation error."}, {"start": 620, "end": 620, "code": "    dot = (t_sim[:, 3:7] * q_gt_xyzw).sum(-1).abs().clamp(max=1.0)", "explanation": "Take the absolute quaternion dot product so q and −q represent the same rotation. Clamp the upper end to keep acos numerically valid."}, {"start": 621, "end": 622, "code": "    dR = 2.0 * torch.acos(dot)\n    return dp, dR", "explanation": "Twice acos of the absolute dot gives the shortest quaternion rotation angle. Return translation and rotation separately."}]}, {"symbol": "feasible() / returns()", "path": "scripts/run_refine.py", "start": 169, "end": 190, "operation": "Check position/rotation thresholds for every frame and object; accumulate C − error plus contact bonus until the first violation.", "note": "Reproduction uses separate position/rotation cutoffs and sums object errors; do not equate this with every weighting in Appendix C.", "key": "scripts/run_refine.py:169", "contract": "Input: per-object position/rotation error arrays [N,H], thresholds Cp and CR, reward baseline C and contact bonus. Output: feasibility per candidate and accumulated return.", "docstring": "(N,) bool: env stayed within thresholds for all frames, both objects.", "example": "If frame 2 first violates a threshold, alive=[true,true,false,false,…]. Even if the object later recovers, those later rewards remain zero in returns().", "steps": [{"start": 172, "end": 175, "code": "    n = next(iter(res[\"dp\"].values())).shape[0]\n    ok = torch.ones(n, dtype=torch.bool, device=next(iter(res[\"dp\"].values())).device)\n    for r in res[\"dp\"]:\n        ok &= (res[\"dp\"][r] <= Cp).all(dim=1) & (res[\"dR\"][r] <= CR).all(dim=1)", "explanation": "Start each candidate as feasible, then require every frame of every object to satisfy both position and rotation limits. One violation makes that candidate infeasible."}, {"start": 184, "end": 184, "code": "    e_t = sum(torch.sqrt(res[\"dp\"][r] ** 2 + res[\"dR\"][r] ** 2) for r in res[\"dp\"])", "explanation": "For each object compute sqrt(dp²+dR²), then sum across objects. This reproduction combines meters and radians directly here."}, {"start": 185, "end": 187, "code": "    viol = torch.zeros_like(e_t, dtype=torch.bool)\n    for r in res[\"dp\"]:\n        viol |= (res[\"dp\"][r] > Cp) | (res[\"dR\"][r] > CR)", "explanation": "Construct a per-frame violation mask when either threshold is exceeded for any object."}, {"start": 188, "end": 188, "code": "    alive = ~viol.cumsum(dim=1).bool()  # True until first violation (incl.)", "explanation": "Cumulative sum remembers the first failure. Negating its Boolean conversion produces an alive mask that is false at the violating frame and all later frames."}, {"start": 189, "end": 190, "code": "    r_t = (C - e_t) + c_contact * res[\"contact\"].float()\n    return (r_t * alive.float()).sum(dim=1), alive", "explanation": "Compute C−error plus contact bonus, zero invalid-time rewards, then sum over time to rank candidates."}]}]}, {"id": "A3.SS2.p1.1", "title": "Track the object and stop on failure", "inputs": "Simulated and demonstrated object poses; threshold C", "outputs": "Tracking reward while the trajectory remains valid", "pseudo": "d_position ← ‖p_sim − p_reference‖₂\nd_rotation ← geodesic_SO3(R_sim, R_reference)\nerror ← paper_tracking_error(d_position, d_rotation)\nif error > C: terminate rollout\notherwise: reward ← C − error", "explanation": "Compare simulated and demonstrated translations with Euclidean distance. Measure the shortest angular discrepancy between rotations. Use the object-tracking definition and weights of the surrounding paper equation. The paper describes early termination at the tracking boundary. Lower object error earns more reward within the valid regime.", "note": "The reproduction uses separate position/rotation thresholds. Its RL reward masking and rollout scorer also differ at the first violating frame; see Python below.", "implementations": [{"symbol": "pose_error_batched()", "path": "src/egoengine/sim/scene.py", "start": 611, "end": 622, "operation": "Compute Euclidean translation error and quaternion geodesic rotation error, 2·acos(|q·q_ref|).", "note": "", "key": "src/egoengine/sim/scene.py:611", "contract": "Input: N simulated poses [N,7] (xyz + quaternion xyzw) and one reference 4×4 transform. Output: dp in meters and dR in radians for each environment.", "docstring": "dp (N,), dR (N,) torch. t_sim (N,7) xyz+xyzw; gt_T (4,4) numpy.", "example": "Identical positions give dp=0. Identical rotations give |dot|=1 and dR=0; a 180° mismatch gives |dot|=0 and dR=π.", "steps": [{"start": 616, "end": 618, "code": "    p_gt = torch.tensor(gt_T[:3, 3], dtype=torch.float32, device=dev)\n    q_gt_xyzw = torch.tensor(Rotation.from_matrix(gt_T[:3, :3]).as_quat(),\n                             dtype=torch.float32, device=dev)", "explanation": "Convert the reference translation and rotation matrix to tensors on the simulation device, using xyzw quaternion order."}, {"start": 619, "end": 619, "code": "    dp = (t_sim[:, :3] - p_gt).norm(dim=-1)", "explanation": "Subtract reference position and take the vector norm: this is the Euclidean translation error."}, {"start": 620, "end": 620, "code": "    dot = (t_sim[:, 3:7] * q_gt_xyzw).sum(-1).abs().clamp(max=1.0)", "explanation": "Take the absolute quaternion dot product so q and −q represent the same rotation. Clamp the upper end to keep acos numerically valid."}, {"start": 621, "end": 622, "code": "    dR = 2.0 * torch.acos(dot)\n    return dp, dR", "explanation": "Twice acos of the absolute dot gives the shortest quaternion rotation angle. Return translation and rotation separately."}]}, {"symbol": "feasible() / returns()", "path": "scripts/run_refine.py", "start": 169, "end": 190, "operation": "Check position/rotation thresholds for every frame and object; accumulate C − error plus contact bonus until the first violation.", "note": "Reproduction uses separate position/rotation cutoffs and sums object errors; do not equate this with every weighting in Appendix C.", "key": "scripts/run_refine.py:169", "contract": "Input: per-object position/rotation error arrays [N,H], thresholds Cp and CR, reward baseline C and contact bonus. Output: feasibility per candidate and accumulated return.", "docstring": "(N,) bool: env stayed within thresholds for all frames, both objects.", "example": "If frame 2 first violates a threshold, alive=[true,true,false,false,…]. Even if the object later recovers, those later rewards remain zero in returns().", "steps": [{"start": 172, "end": 175, "code": "    n = next(iter(res[\"dp\"].values())).shape[0]\n    ok = torch.ones(n, dtype=torch.bool, device=next(iter(res[\"dp\"].values())).device)\n    for r in res[\"dp\"]:\n        ok &= (res[\"dp\"][r] <= Cp).all(dim=1) & (res[\"dR\"][r] <= CR).all(dim=1)", "explanation": "Start each candidate as feasible, then require every frame of every object to satisfy both position and rotation limits. One violation makes that candidate infeasible."}, {"start": 184, "end": 184, "code": "    e_t = sum(torch.sqrt(res[\"dp\"][r] ** 2 + res[\"dR\"][r] ** 2) for r in res[\"dp\"])", "explanation": "For each object compute sqrt(dp²+dR²), then sum across objects. This reproduction combines meters and radians directly here."}, {"start": 185, "end": 187, "code": "    viol = torch.zeros_like(e_t, dtype=torch.bool)\n    for r in res[\"dp\"]:\n        viol |= (res[\"dp\"][r] > Cp) | (res[\"dR\"][r] > CR)", "explanation": "Construct a per-frame violation mask when either threshold is exceeded for any object."}, {"start": 188, "end": 188, "code": "    alive = ~viol.cumsum(dim=1).bool()  # True until first violation (incl.)", "explanation": "Cumulative sum remembers the first failure. Negating its Boolean conversion produces an alive mask that is false at the violating frame and all later frames."}, {"start": 189, "end": 190, "code": "    r_t = (C - e_t) + c_contact * res[\"contact\"].float()\n    return (r_t * alive.float()).sum(dim=1), alive", "explanation": "Compute C−error plus contact bonus, zero invalid-time rewards, then sum over time to rank candidates."}]}]}, {"id": "S3.SS2.SSS2.p3.1", "title": "Choose among Replay, MPC and residual RL", "inputs": "Current state, reference commands and object-motion target", "outputs": "A candidate robot rollout", "pseudo": "Replay: execute the reference commands in simulation\nMPC: sample short-horizon actions around the reference\n     score candidates by object tracking; refine the search\nRL observation ← hand_state, object_pose, reference_command\nδa ← residual_policy(observation)\na ← reference_command ⊕ δa\ntrain residual_policy with PPO on the difficult chunk", "explanation": "Use the reference directly as the cheapest solver. Explore local corrections rather than replacing the motion prior. Use simulated object motion to choose a better candidate. Condition the policy on current state and the motion prior. Let the learned policy correct the reference command. Add residuals in the action representation; orientation corrections require rotation composition. Learn corrections for chunks that cheaper solvers cannot handle.", "note": "The paper specifies sampled MPC; the reproduction chooses an MPPI-style softmax update. PPO training is separate from checkpoint loading in the controller.", "implementations": [{"symbol": "rollout()", "path": "scripts/run_refine.py", "start": 124, "end": 149, "operation": "Apply reference wrist/finger targets plus optional deltas, step physics, and measure the resulting object poses.", "note": "", "key": "scripts/run_refine.py:124", "contract": "Rollout loop: N parallel simulator environments over a frame window. Optional corrections have shapes [N,H,3] for wrist translation and [N,H,12] for fingers.", "docstring": "Roll all envs from frame t0 to t1 (exclusive). act_seqs: optional\n{side: (wrist_dxyz (N,H,3)T, q12_d (N,H,12)T)} deltas on the reference.\nReturns dict with per-frame per-env dp/dR (N,H) per role, contact flags,\nand env-0 executed/actual states if record.", "example": "With act_seqs=None this is Replay. Supplying sampled deltas reuses the same simulator loop to evaluate MPC candidates.", "steps": [{"start": 124, "end": 127, "code": "    prev = None\n    for k in range(hlen):\n        t = t0 + k\n        ra = ref_actions(prep, t)", "explanation": "Start a frame window with no previous command and retrieve the retargeted reference action at each frame."}, {"start": 129, "end": 133, "code": "        for s in SIDES:\n            p = torch.tensor(ra[s][0], dtype=torch.float32, device=dev)[None].expand(n, 3)\n            q = torch.tensor(ra[s][1], dtype=torch.float32, device=dev)[None].expand(n, 4)\n            v = torch.tensor(ra[s][2], dtype=torch.float32, device=dev)[None].expand(n, 3)\n            q12 = torch.tensor(ra[s][3], dtype=torch.float32, device=dev)[None].expand(n, 12)", "explanation": "Broadcast the reference wrist position, quaternion, velocity and 12 finger targets to all candidate environments."}, {"start": 134, "end": 137, "code": "            if act_seqs is not None:\n                p = p + act_seqs[s][0][:, k]\n                q12 = q12 + act_seqs[s][1][:, k]\n            acts[s] = (p, q, v, q12)", "explanation": "If candidate corrections exist, add them to wrist position and finger targets. This MPC path does not change the wrist quaternion."}, {"start": 138, "end": 142, "code": "        for sub in range(1, substeps + 1):\n            if prev is not None:\n                apply_action_frame(H, acts, prev, sub / substeps)\n            else:\n                apply_action_frame(H, acts, acts, 1.0)", "explanation": "Interpolate from the previous action over physics substeps; for the first frame, apply the current action directly."}, {"start": 143, "end": 145, "code": "            H[\"physx\"].simulate(H[\"dt\"], 0.0)\n            H[\"physx\"].fetch_results()\n        prev = acts", "explanation": "Advance PhysX and fetch results before reading state. These calls make the reference into a physical rollout rather than just a pose sequence."}, {"start": 146, "end": 149, "code": "        for r in roles:\n            ts = get_object_poses_batched(H, r)\n            dp[r][:, k], dR[r][:, k] = pose_error_batched(\n                ts, prep[\"objects\"][r][\"gt\"][t])", "explanation": "Read each simulated object pose and compare it with the demonstration at the same frame; store translation and rotation error for feasibility and scoring."}]}, {"symbol": "MPC sampling loop", "path": "scripts/run_refine.py", "start": 370, "end": 406, "operation": "Sample Gaussian wrist/finger corrections, score simulated rollouts, update the mean with softmax return weights, and retain the best sample.", "note": "", "key": "scripts/run_refine.py:370", "contract": "MPC searches wrist/finger correction sequences over the two-chunk window. Each simulator environment evaluates one sampled candidate.", "docstring": null, "example": "A high-return candidate can still violate the boundary late in the window. The best-return selection and feasible-sample count are separate quantities in this code.", "steps": [{"start": 372, "end": 377, "code": "            hlen = t_win - t\n            mu = {s: (torch.zeros(num_envs, hlen, 3, device=dev),\n                      torch.zeros(num_envs, hlen, 12, device=dev)) for s in SIDES}\n            scales = {s: (torch.full((1, 1, 3), SIGMA_WRIST, device=dev),\n                          torch.full((1, 1, 12), SIGMA_FINGER, device=dev))\n                      for s in SIDES}", "explanation": "Set the window length, initialize correction means to zero, and choose separate physical noise scales for wrist and finger coordinates."}, {"start": 380, "end": 384, "code": "            for it in range(args.mpc_iters):\n                sig = args.sigma0 * (0.5 ** it)\n                deltas = {s: (mu[s][0] + sig * scales[s][0] * torch.randn(num_envs, hlen, 3, device=dev),\n                              mu[s][1] + sig * scales[s][1] * torch.randn(num_envs, hlen, 12, device=dev))\n                          for s in SIDES}", "explanation": "At each search iteration halve sigma and draw Gaussian perturbations around the current mean. The reference remains the center of the initial search."}, {"start": 385, "end": 389, "code": "                reset_envs_state(H, state[\"qpos\"], state[\"obj_T\"])\n                res = rollout(H, prep, t, t_win, args.substeps, act_seqs=deltas,\n                              reward_cfg={\"contact_thresh\": args.contact_thresh},\n                              tool_verts=tool_verts)\n                J, _ = returns(res, C, args.c_contact, Cp, CR)", "explanation": "Reset every candidate to the identical chunk-start state, simulate its corrections, and compute the return; this makes candidate scores comparable."}, {"start": 390, "end": 394, "code": "                jmax = J.max()\n                if float(jmax) > best_J:\n                    best_J = float(jmax)\n                    bi = int(J.argmax())\n                    best_delta = {s: (deltas[s][0][bi], deltas[s][1][bi]) for s in SIDES}", "explanation": "Keep the highest-return sampled sequence found so far. Selection here maximizes return rather than explicitly restricting the choice to feasible samples."}, {"start": 395, "end": 396, "code": "                n_feas = int(feasible(res, Cp, CR).sum())\n                max_feas = max(max_feas, n_feas)", "explanation": "Count feasible candidates separately, retaining whether any search iteration found one."}, {"start": 398, "end": 401, "code": "                w = torch.softmax((J - jmax) / max(1e-3, 0.1 * float(J.std() + 1e-6)), dim=0)\n                for s in SIDES:\n                    mu[s] = ((w[:, None, None] * deltas[s][0]).sum(0, keepdim=True),\n                             (w[:, None, None] * deltas[s][1]).sum(0, keepdim=True))", "explanation": "Convert returns to softmax weights and take a weighted mean of sampled corrections. This MPPI-style update steers later samples toward better rollouts."}, {"start": 404, "end": 406, "code": "            crec[\"mpc_best_return\"] = best_J\n            crec[\"mpc_max_feasible_samples\"] = max_feas\n            chosen = {s: (best_delta[s][0][None], best_delta[s][1][None]) for s in SIDES}", "explanation": "Record search diagnostics and keep the best correction sequence for execution."}]}, {"symbol": "residual_to_actions() / compute_obs()", "path": "src/egoengine/rl/chunk_env.py", "start": 99, "end": 158, "operation": "Concatenate hand/object/reference state; add translation and finger residuals and compose the rotation residual with the reference quaternion.", "note": "", "key": "src/egoengine/rl/chunk_env.py:99", "contract": "Residual action: [N,36], with 18 values per hand (3 translation, 3 rotation-vector, 12 fingers). Observation: hand state, current/reference object poses and retargeted hand commands.", "docstring": "a_t = a_base + δa_t. residual: (N,36) already in PHYSICAL units\n(multiply normalized policy output by ACT_SCALES first).", "example": "A +0.01 m x residual shifts the wrist target by 1 cm. Rotation residuals cannot simply be added to quaternion components; they are composed as rotations.", "steps": [{"start": 103, "end": 108, "code": "    base = ref_base_actions(prep, t, n, dev)\n    acts = {}\n    for i, s in enumerate(SIDES):\n        sl = slice(i * HAND_ACT, (i + 1) * HAND_ACT)\n        dxyz, drot, dfing = residual[:, sl][:,:3], residual[:, sl][:,3:6], residual[:, sl][:,6:]\n        p, q, v, q12 = base[s]", "explanation": "Retrieve the base action, slice the policy output into left/right hands, and split each into translation, rotation and finger corrections."}, {"start": 109, "end": 111, "code": "        q_new = _quat_mul_wxyz(_rotvec_to_quat_wxyz(drot), q)\n        q_new = q_new / q_new.norm(dim=-1, keepdim=True).clamp(min=1e-9)\n        acts[s] = (p + dxyz, q_new, v, q12 + dfing)", "explanation": "Convert the rotation vector to a quaternion and compose it with the reference orientation. Normalize it; add the other residuals to position and finger targets."}, {"start": 130, "end": 132, "code": "    n = H[\"num_envs\"]\n    origins = torch.arange(n, dtype=torch.float32, device=dev)[:, None] * \\\n        torch.tensor([H[\"spacing\"], 0.0, 0.0], device=dev)", "explanation": "Compute environment origins so observations can remove the spatial offsets used to separate parallel simulations."}, {"start": 134, "end": 145, "code": "    for s in SIDES:\n        av = H[\"art\"][s]\n        lt = wp.to_torch(av.get_link_transforms()).clone().to(dev)\n        lv = wp.to_torch(av.get_link_velocities()).clone().to(dev)\n        qp = wp.to_torch(av.get_dof_positions()).clone().to(dev)\n        qv = wp.to_torch(av.get_dof_velocities()).clone().to(dev)\n        root_p = lt[:, 0, :3] - origins\n        rq = lt[:, 0, 3:7]  # xyzw -> wxyz\n        root_q = torch.stack([rq[:, 3], rq[:, 0], rq[:, 1], rq[:, 2]], dim=-1)\n        q12 = qp[:, H[\"inv_joint_idx\"][s]]\n        qd12 = qv[:, H[\"inv_joint_idx\"][s]]\n        parts += [root_p, root_q, q12, lv[:, 0, :6], qd12]", "explanation": "Read hand link poses/velocities and finger positions/velocities. Reorder the root quaternion from xyzw to wxyz before concatenating the hand state."}, {"start": 147, "end": 153, "code": "    for r in (\"tool\", \"target\"):\n        ts = get_object_poses_batched(H, r)  # (N,7) xyz + quat xyzw, local frame\n        gt = prep[\"objects\"][r][\"gt\"][t]\n        p_ref = torch.tensor(gt[:3, 3], dtype=torch.float32, device=dev)[None].expand(n, 3)\n        q_ref = torch.tensor(Rotation.from_matrix(gt[:3, :3]).as_quat(),\n                             dtype=torch.float32, device=dev)[None].expand(n, 4)\n        parts += [ts[:, :3], ts[:, 3:7], p_ref, q_ref]", "explanation": "Append both the current simulated and desired object poses. The residual policy can therefore observe deviation from the demonstration."}, {"start": 154, "end": 158, "code": "    for s in SIDES:\n        q = torch.tensor(prep[\"qpos\"][s][t], dtype=torch.float32, device=dev)\n        parts += [q[:3][None].expand(n, 3), q[3:7][None].expand(n, 4),\n                  q[7:][None].expand(n, 12)]\n    return torch.cat(parts, dim=-1)", "explanation": "Append reference hand commands and concatenate the observation parts along the feature dimension."}]}, {"symbol": "OnPolicyRunner / runner.learn()", "path": "scripts/train_residual.py", "start": 280, "end": 318, "operation": "Configure rsl_rl PPO and train the residual policy for one failed chunk.", "note": "", "key": "scripts/train_residual.py:280", "contract": "Training setup for one failed chunk: an ActorCritic residual policy optimized by the external rsl_rl PPO implementation.", "docstring": null, "example": "The adaptive controller loads checkpoints produced by this training script. It does not launch this PPO training automatically at each escalation.", "steps": [{"start": 283, "end": 288, "code": "            \"class_name\": \"ActorCritic\",\n            \"init_noise_std\": 0.6,\n            \"actor_hidden_dims\": [256, 256],\n            \"critic_hidden_dims\": [256, 256],\n            \"activation\": \"elu\",\n        },", "explanation": "Use two 256-unit hidden layers for actor and critic with ELU activation; initialize policy noise with standard deviation 0.6."}, {"start": 290, "end": 295, "code": "            \"class_name\": \"PPO\",\n            \"value_loss_coef\": 1.0,\n            \"use_clipped_value_loss\": True,\n            \"clip_param\": 0.2,\n            \"entropy_coef\": 0.002,\n            \"num_learning_epochs\": 4,", "explanation": "Select PPO, clipped value loss, policy clipping 0.2, and an entropy bonus. The actual PPO update lives inside rsl_rl, not this script."}, {"start": 296, "end": 303, "code": "            \"num_mini_batches\": 4,\n            \"learning_rate\": 3.0e-4,\n            \"schedule\": \"adaptive\",\n            \"gamma\": 0.99,\n            \"lam\": 0.95,\n            \"desired_kl\": 0.02,\n            \"max_grad_norm\": 1.0,\n        },", "explanation": "Configure epochs/minibatches, learning rate, discount γ, advantage parameter λ, KL target and gradient clipping."}, {"start": 306, "end": 307, "code": "    from rsl_rl.runners import OnPolicyRunner\n    runner = OnPolicyRunner(env, train_cfg, log_dir=str(log_dir), device=H[\"device\"])", "explanation": "Bind the prepared simulation environment and configuration to OnPolicyRunner, which collects rollouts and performs updates."}, {"start": 310, "end": 312, "code": "    t_learn = time.time()\n    it_done = 0\n    while it_done < args.iters and (time.time() - t_learn) < args.max_minutes * 60:", "explanation": "Stop when either the iteration budget or wall-clock budget is exhausted."}, {"start": 313, "end": 318, "code": "        if cur_on:\n            # anneal from start-scale to 1.0 over the first half of --iters\n            f = min(1.0, it_done / max(1, 0.5 * args.iters))\n            s = args.curriculum_start_scale + (1.0 - args.curriculum_start_scale) * f\n            env.set_thresholds(Cp * s, CR * s)\n        runner.learn(num_learning_iterations=1, init_at_random_ep_len=False)", "explanation": "If curriculum is enabled, anneal feasibility thresholds toward the target values; then perform one learning iteration."}]}]}, {"id": "S3.F2", "title": "Adaptively switch solvers over two chunks", "inputs": "Current simulator state, reference trajectory, chunk length H", "outputs": "Feasible actions for the current chunk", "pseudo": "for each chunk boundary t:\n    window ← reference[t : t + 2H]\n    for solver in [Replay, MPC, RL]:\n        candidate ← solver.optimize(state, window)\n        if feasible(candidate over the entire window):\n            execute(candidate[0 : H])\n            state ← resulting simulator state\n            break\n    if no solver is feasible: leave this chunk unresolved", "explanation": "Reconsider solver choice from the state reached after the previous chunk. Include the next chunk to avoid a locally good but unusable boundary state. This is heuristic progressive search, not a full MCTS tree with backups. Replay follows the prior; MPC searches locally; RL supplies learned residual correction. Accept a solver only when its rollout satisfies the object-tracking criterion. Commit only the current chunk, leaving the next chunk open to replanning. Carry the physically reached state into the next planning cycle. A feasible cheaper solver avoids unnecessary use of a more expensive solver. No feasible action is implied when every solver fails; retry/abort policy is not specified here.", "note": "The unresolved branch is an explicit reading aid, not a specified recovery algorithm. The reproduction can execute its best MPC candidate even when infeasible; RL fallback requires a checkpoint.", "implementations": [{"symbol": "Adaptive chunk loop", "path": "scripts/run_refine.py", "start": 351, "end": 434, "operation": "Plan over two chunks: try Replay → sample MPC corrections → optionally load an RL policy; execute only the current chunk.", "note": "RL requires --mode rl and a separately trained checkpoint. The loop can execute the best MPC candidate even if no sample is feasible, unlike the paper’s feasibility-gated description.", "key": "scripts/run_refine.py:351", "contract": "Controller state machine: optimize a 2H-step window, execute H steps, then repeat from the resulting physical state.", "docstring": null, "example": "For H=20 at t=40, candidate rollouts cover [40,80), but execution covers [40,60). A missing RL checkpoint leaves the MPC choice in place.", "steps": [{"start": 351, "end": 355, "code": "    t = f0\n    while t < f1:\n        t_end = min(t + Hn, f1)\n        t_win = min(t + 2 * Hn, f1)  # two-chunk lookahead window\n        crec = {\"t0\": t, \"t1\": t_end, \"window\": [t, t_win]}", "explanation": "Compute the current chunk end and the two-chunk lookahead end, clipping both at sequence length."}, {"start": 357, "end": 363, "code": "        # 1) Replay feasibility check over the two-chunk window\n        reset_envs_state(H, state[\"qpos\"], state[\"obj_T\"])\n        res = rollout(H, prep, t, t_win, args.substeps)\n        ok = feasible(res, Cp, CR)\n        crec[\"replay_feasible_window\"] = bool(ok[0].item())\n        ret_replay, _ = returns(res, C, args.c_contact, Cp, CR)\n        crec[\"replay_return\"] = float(ret_replay[0].item())", "explanation": "Reset to the saved boundary state, replay the reference through the entire lookahead, and record feasibility and return."}, {"start": 365, "end": 371, "code": "        chosen = None  # action deltas for the EXECUTED chunk\n        exec_policy = None  # residual policy for the EXECUTED chunk (rl mode)\n        if args.mode == \"replay\" or crec[\"replay_feasible_window\"]:\n            crec[\"mode\"] = \"replay\"\n        else:\n            # 2) MPC over the window (plan 2H, execute H)\n            crec[\"mode\"] = \"mpc\"", "explanation": "Keep Replay when it passes, or when replay-only mode is requested. Otherwise enter the MPC search."}, {"start": 380, "end": 389, "code": "            for it in range(args.mpc_iters):\n                sig = args.sigma0 * (0.5 ** it)\n                deltas = {s: (mu[s][0] + sig * scales[s][0] * torch.randn(num_envs, hlen, 3, device=dev),\n                              mu[s][1] + sig * scales[s][1] * torch.randn(num_envs, hlen, 12, device=dev))\n                          for s in SIDES}\n                reset_envs_state(H, state[\"qpos\"], state[\"obj_T\"])\n                res = rollout(H, prep, t, t_win, args.substeps, act_seqs=deltas,\n                              reward_cfg={\"contact_thresh\": args.contact_thresh},\n                              tool_verts=tool_verts)\n                J, _ = returns(res, C, args.c_contact, Cp, CR)", "explanation": "Sample wrist/finger corrections, reset to the same initial state, and score each candidate over the lookahead window."}, {"start": 390, "end": 401, "code": "                jmax = J.max()\n                if float(jmax) > best_J:\n                    best_J = float(jmax)\n                    bi = int(J.argmax())\n                    best_delta = {s: (deltas[s][0][bi], deltas[s][1][bi]) for s in SIDES}\n                n_feas = int(feasible(res, Cp, CR).sum())\n                max_feas = max(max_feas, n_feas)\n                # MPPI update\n                w = torch.softmax((J - jmax) / max(1e-3, 0.1 * float(J.std() + 1e-6)), dim=0)\n                for s in SIDES:\n                    mu[s] = ((w[:, None, None] * deltas[s][0]).sum(0, keepdim=True),\n                             (w[:, None, None] * deltas[s][1]).sum(0, keepdim=True))", "explanation": "Track the best sampled action sequence and update the sampling mean from softmax-weighted returns."}, {"start": 408, "end": 418, "code": "            # 2b) RL escalation: MPC found no feasible sample -> residual policy\n            if args.mode == \"rl\" and max_feas == 0:\n                from egoengine.rl.chunk_env import OBS_DIM, ACT_DIM\n                pol = load_chunk_policy(args.triplet, args.seq_id, t, t_end,\n                                        dev, OBS_DIM, ACT_DIM)\n                if pol is not None:\n                    crec[\"mode\"] = \"rl\"\n                    crec[\"rl_checkpoint\"] = \\\n                        f\"outputs/rl/{args.triplet}/{args.seq_id}/chunk_{t}_{t_end}\"\n                    exec_policy = pol\n                    chosen = None", "explanation": "Escalate only in rl mode and when MPC found no feasible sample. Load a chunk checkpoint; if one exists, replace the chosen MPC sequence with that residual policy."}, {"start": 424, "end": 434, "code": "        if chosen is not None:\n            exec_deltas = chosen  # (1,H,·) broadcast happens via expand below\n            # expand to all envs for batched stepping; only env 0 is recorded\n            exec_deltas = {s: (chosen[s][0].expand(H[\"num_envs\"], -1, -1),\n                               chosen[s][1].expand(H[\"num_envs\"], -1, -1)) for s in SIDES}\n        reset_envs_state(H, state[\"qpos\"], state[\"obj_T\"])\n        if exec_policy is not None:\n            res = rl_rollout(H, prep, t, t_end, args.substeps, exec_policy)\n        else:\n            res = rollout(H, prep, t, t_end, args.substeps, act_seqs=exec_deltas,\n                          record=True)", "explanation": "Reset again and execute only t…t_end, either using the residual policy or the chosen corrections. The next chunk is reconsidered rather than committed now."}]}]}, {"id": "S3.SS2.SSS2.p4.1", "title": "Adaptively switch solvers over two chunks", "inputs": "Current simulator state, reference trajectory, chunk length H", "outputs": "Feasible actions for the current chunk", "pseudo": "for each chunk boundary t:\n    window ← reference[t : t + 2H]\n    for solver in [Replay, MPC, RL]:\n        candidate ← solver.optimize(state, window)\n        if feasible(candidate over the entire window):\n            execute(candidate[0 : H])\n            state ← resulting simulator state\n            break\n    if no solver is feasible: leave this chunk unresolved", "explanation": "Reconsider solver choice from the state reached after the previous chunk. Include the next chunk to avoid a locally good but unusable boundary state. This is heuristic progressive search, not a full MCTS tree with backups. Replay follows the prior; MPC searches locally; RL supplies learned residual correction. Accept a solver only when its rollout satisfies the object-tracking criterion. Commit only the current chunk, leaving the next chunk open to replanning. Carry the physically reached state into the next planning cycle. A feasible cheaper solver avoids unnecessary use of a more expensive solver. No feasible action is implied when every solver fails; retry/abort policy is not specified here.", "note": "The unresolved branch is an explicit reading aid, not a specified recovery algorithm. The reproduction can execute its best MPC candidate even when infeasible; RL fallback requires a checkpoint.", "implementations": [{"symbol": "Adaptive chunk loop", "path": "scripts/run_refine.py", "start": 351, "end": 434, "operation": "Plan over two chunks: try Replay → sample MPC corrections → optionally load an RL policy; execute only the current chunk.", "note": "RL requires --mode rl and a separately trained checkpoint. The loop can execute the best MPC candidate even if no sample is feasible, unlike the paper’s feasibility-gated description.", "key": "scripts/run_refine.py:351", "contract": "Controller state machine: optimize a 2H-step window, execute H steps, then repeat from the resulting physical state.", "docstring": null, "example": "For H=20 at t=40, candidate rollouts cover [40,80), but execution covers [40,60). A missing RL checkpoint leaves the MPC choice in place.", "steps": [{"start": 351, "end": 355, "code": "    t = f0\n    while t < f1:\n        t_end = min(t + Hn, f1)\n        t_win = min(t + 2 * Hn, f1)  # two-chunk lookahead window\n        crec = {\"t0\": t, \"t1\": t_end, \"window\": [t, t_win]}", "explanation": "Compute the current chunk end and the two-chunk lookahead end, clipping both at sequence length."}, {"start": 357, "end": 363, "code": "        # 1) Replay feasibility check over the two-chunk window\n        reset_envs_state(H, state[\"qpos\"], state[\"obj_T\"])\n        res = rollout(H, prep, t, t_win, args.substeps)\n        ok = feasible(res, Cp, CR)\n        crec[\"replay_feasible_window\"] = bool(ok[0].item())\n        ret_replay, _ = returns(res, C, args.c_contact, Cp, CR)\n        crec[\"replay_return\"] = float(ret_replay[0].item())", "explanation": "Reset to the saved boundary state, replay the reference through the entire lookahead, and record feasibility and return."}, {"start": 365, "end": 371, "code": "        chosen = None  # action deltas for the EXECUTED chunk\n        exec_policy = None  # residual policy for the EXECUTED chunk (rl mode)\n        if args.mode == \"replay\" or crec[\"replay_feasible_window\"]:\n            crec[\"mode\"] = \"replay\"\n        else:\n            # 2) MPC over the window (plan 2H, execute H)\n            crec[\"mode\"] = \"mpc\"", "explanation": "Keep Replay when it passes, or when replay-only mode is requested. Otherwise enter the MPC search."}, {"start": 380, "end": 389, "code": "            for it in range(args.mpc_iters):\n                sig = args.sigma0 * (0.5 ** it)\n                deltas = {s: (mu[s][0] + sig * scales[s][0] * torch.randn(num_envs, hlen, 3, device=dev),\n                              mu[s][1] + sig * scales[s][1] * torch.randn(num_envs, hlen, 12, device=dev))\n                          for s in SIDES}\n                reset_envs_state(H, state[\"qpos\"], state[\"obj_T\"])\n                res = rollout(H, prep, t, t_win, args.substeps, act_seqs=deltas,\n                              reward_cfg={\"contact_thresh\": args.contact_thresh},\n                              tool_verts=tool_verts)\n                J, _ = returns(res, C, args.c_contact, Cp, CR)", "explanation": "Sample wrist/finger corrections, reset to the same initial state, and score each candidate over the lookahead window."}, {"start": 390, "end": 401, "code": "                jmax = J.max()\n                if float(jmax) > best_J:\n                    best_J = float(jmax)\n                    bi = int(J.argmax())\n                    best_delta = {s: (deltas[s][0][bi], deltas[s][1][bi]) for s in SIDES}\n                n_feas = int(feasible(res, Cp, CR).sum())\n                max_feas = max(max_feas, n_feas)\n                # MPPI update\n                w = torch.softmax((J - jmax) / max(1e-3, 0.1 * float(J.std() + 1e-6)), dim=0)\n                for s in SIDES:\n                    mu[s] = ((w[:, None, None] * deltas[s][0]).sum(0, keepdim=True),\n                             (w[:, None, None] * deltas[s][1]).sum(0, keepdim=True))", "explanation": "Track the best sampled action sequence and update the sampling mean from softmax-weighted returns."}, {"start": 408, "end": 418, "code": "            # 2b) RL escalation: MPC found no feasible sample -> residual policy\n            if args.mode == \"rl\" and max_feas == 0:\n                from egoengine.rl.chunk_env import OBS_DIM, ACT_DIM\n                pol = load_chunk_policy(args.triplet, args.seq_id, t, t_end,\n                                        dev, OBS_DIM, ACT_DIM)\n                if pol is not None:\n                    crec[\"mode\"] = \"rl\"\n                    crec[\"rl_checkpoint\"] = \\\n                        f\"outputs/rl/{args.triplet}/{args.seq_id}/chunk_{t}_{t_end}\"\n                    exec_policy = pol\n                    chosen = None", "explanation": "Escalate only in rl mode and when MPC found no feasible sample. Load a chunk checkpoint; if one exists, replace the chosen MPC sequence with that residual policy."}, {"start": 424, "end": 434, "code": "        if chosen is not None:\n            exec_deltas = chosen  # (1,H,·) broadcast happens via expand below\n            # expand to all envs for batched stepping; only env 0 is recorded\n            exec_deltas = {s: (chosen[s][0].expand(H[\"num_envs\"], -1, -1),\n                               chosen[s][1].expand(H[\"num_envs\"], -1, -1)) for s in SIDES}\n        reset_envs_state(H, state[\"qpos\"], state[\"obj_T\"])\n        if exec_policy is not None:\n            res = rl_rollout(H, prep, t, t_end, args.substeps, exec_policy)\n        else:\n            res = rollout(H, prep, t, t_end, args.substeps, act_seqs=exec_deltas,\n                          record=True)", "explanation": "Reset again and execute only t…t_end, either using the residual policy or the chosen corrections. The next chunk is reconsidered rather than committed now."}]}]}, {"id": "A3.SS1.p1.1", "title": "Adaptively switch solvers over two chunks", "inputs": "Current simulator state, reference trajectory, chunk length H", "outputs": "Feasible actions for the current chunk", "pseudo": "for each chunk boundary t:\n    window ← reference[t : t + 2H]\n    for solver in [Replay, MPC, RL]:\n        candidate ← solver.optimize(state, window)\n        if feasible(candidate over the entire window):\n            execute(candidate[0 : H])\n            state ← resulting simulator state\n            break\n    if no solver is feasible: leave this chunk unresolved", "explanation": "Reconsider solver choice from the state reached after the previous chunk. Include the next chunk to avoid a locally good but unusable boundary state. This is heuristic progressive search, not a full MCTS tree with backups. Replay follows the prior; MPC searches locally; RL supplies learned residual correction. Accept a solver only when its rollout satisfies the object-tracking criterion. Commit only the current chunk, leaving the next chunk open to replanning. Carry the physically reached state into the next planning cycle. A feasible cheaper solver avoids unnecessary use of a more expensive solver. No feasible action is implied when every solver fails; retry/abort policy is not specified here.", "note": "The unresolved branch is an explicit reading aid, not a specified recovery algorithm. The reproduction can execute its best MPC candidate even when infeasible; RL fallback requires a checkpoint.", "implementations": [{"symbol": "Adaptive chunk loop", "path": "scripts/run_refine.py", "start": 351, "end": 434, "operation": "Plan over two chunks: try Replay → sample MPC corrections → optionally load an RL policy; execute only the current chunk.", "note": "RL requires --mode rl and a separately trained checkpoint. The loop can execute the best MPC candidate even if no sample is feasible, unlike the paper’s feasibility-gated description.", "key": "scripts/run_refine.py:351", "contract": "Controller state machine: optimize a 2H-step window, execute H steps, then repeat from the resulting physical state.", "docstring": null, "example": "For H=20 at t=40, candidate rollouts cover [40,80), but execution covers [40,60). A missing RL checkpoint leaves the MPC choice in place.", "steps": [{"start": 351, "end": 355, "code": "    t = f0\n    while t < f1:\n        t_end = min(t + Hn, f1)\n        t_win = min(t + 2 * Hn, f1)  # two-chunk lookahead window\n        crec = {\"t0\": t, \"t1\": t_end, \"window\": [t, t_win]}", "explanation": "Compute the current chunk end and the two-chunk lookahead end, clipping both at sequence length."}, {"start": 357, "end": 363, "code": "        # 1) Replay feasibility check over the two-chunk window\n        reset_envs_state(H, state[\"qpos\"], state[\"obj_T\"])\n        res = rollout(H, prep, t, t_win, args.substeps)\n        ok = feasible(res, Cp, CR)\n        crec[\"replay_feasible_window\"] = bool(ok[0].item())\n        ret_replay, _ = returns(res, C, args.c_contact, Cp, CR)\n        crec[\"replay_return\"] = float(ret_replay[0].item())", "explanation": "Reset to the saved boundary state, replay the reference through the entire lookahead, and record feasibility and return."}, {"start": 365, "end": 371, "code": "        chosen = None  # action deltas for the EXECUTED chunk\n        exec_policy = None  # residual policy for the EXECUTED chunk (rl mode)\n        if args.mode == \"replay\" or crec[\"replay_feasible_window\"]:\n            crec[\"mode\"] = \"replay\"\n        else:\n            # 2) MPC over the window (plan 2H, execute H)\n            crec[\"mode\"] = \"mpc\"", "explanation": "Keep Replay when it passes, or when replay-only mode is requested. Otherwise enter the MPC search."}, {"start": 380, "end": 389, "code": "            for it in range(args.mpc_iters):\n                sig = args.sigma0 * (0.5 ** it)\n                deltas = {s: (mu[s][0] + sig * scales[s][0] * torch.randn(num_envs, hlen, 3, device=dev),\n                              mu[s][1] + sig * scales[s][1] * torch.randn(num_envs, hlen, 12, device=dev))\n                          for s in SIDES}\n                reset_envs_state(H, state[\"qpos\"], state[\"obj_T\"])\n                res = rollout(H, prep, t, t_win, args.substeps, act_seqs=deltas,\n                              reward_cfg={\"contact_thresh\": args.contact_thresh},\n                              tool_verts=tool_verts)\n                J, _ = returns(res, C, args.c_contact, Cp, CR)", "explanation": "Sample wrist/finger corrections, reset to the same initial state, and score each candidate over the lookahead window."}, {"start": 390, "end": 401, "code": "                jmax = J.max()\n                if float(jmax) > best_J:\n                    best_J = float(jmax)\n                    bi = int(J.argmax())\n                    best_delta = {s: (deltas[s][0][bi], deltas[s][1][bi]) for s in SIDES}\n                n_feas = int(feasible(res, Cp, CR).sum())\n                max_feas = max(max_feas, n_feas)\n                # MPPI update\n                w = torch.softmax((J - jmax) / max(1e-3, 0.1 * float(J.std() + 1e-6)), dim=0)\n                for s in SIDES:\n                    mu[s] = ((w[:, None, None] * deltas[s][0]).sum(0, keepdim=True),\n                             (w[:, None, None] * deltas[s][1]).sum(0, keepdim=True))", "explanation": "Track the best sampled action sequence and update the sampling mean from softmax-weighted returns."}, {"start": 408, "end": 418, "code": "            # 2b) RL escalation: MPC found no feasible sample -> residual policy\n            if args.mode == \"rl\" and max_feas == 0:\n                from egoengine.rl.chunk_env import OBS_DIM, ACT_DIM\n                pol = load_chunk_policy(args.triplet, args.seq_id, t, t_end,\n                                        dev, OBS_DIM, ACT_DIM)\n                if pol is not None:\n                    crec[\"mode\"] = \"rl\"\n                    crec[\"rl_checkpoint\"] = \\\n                        f\"outputs/rl/{args.triplet}/{args.seq_id}/chunk_{t}_{t_end}\"\n                    exec_policy = pol\n                    chosen = None", "explanation": "Escalate only in rl mode and when MPC found no feasible sample. Load a chunk checkpoint; if one exists, replace the chosen MPC sequence with that residual policy."}, {"start": 424, "end": 434, "code": "        if chosen is not None:\n            exec_deltas = chosen  # (1,H,·) broadcast happens via expand below\n            # expand to all envs for batched stepping; only env 0 is recorded\n            exec_deltas = {s: (chosen[s][0].expand(H[\"num_envs\"], -1, -1),\n                               chosen[s][1].expand(H[\"num_envs\"], -1, -1)) for s in SIDES}\n        reset_envs_state(H, state[\"qpos\"], state[\"obj_T\"])\n        if exec_policy is not None:\n            res = rl_rollout(H, prep, t, t_end, args.substeps, exec_policy)\n        else:\n            res = rollout(H, prep, t, t_end, args.substeps, act_seqs=exec_deltas,\n                          record=True)", "explanation": "Reset again and execute only t…t_end, either using the residual policy or the chosen corrections. The next chunk is reconsidered rather than committed now."}]}]}, {"id": "A3.SS1.p2.1", "title": "Adaptively switch solvers over two chunks", "inputs": "Current simulator state, reference trajectory, chunk length H", "outputs": "Feasible actions for the current chunk", "pseudo": "for each chunk boundary t:\n    window ← reference[t : t + 2H]\n    for solver in [Replay, MPC, RL]:\n        candidate ← solver.optimize(state, window)\n        if feasible(candidate over the entire window):\n            execute(candidate[0 : H])\n            state ← resulting simulator state\n            break\n    if no solver is feasible: leave this chunk unresolved", "explanation": "Reconsider solver choice from the state reached after the previous chunk. Include the next chunk to avoid a locally good but unusable boundary state. This is heuristic progressive search, not a full MCTS tree with backups. Replay follows the prior; MPC searches locally; RL supplies learned residual correction. Accept a solver only when its rollout satisfies the object-tracking criterion. Commit only the current chunk, leaving the next chunk open to replanning. Carry the physically reached state into the next planning cycle. A feasible cheaper solver avoids unnecessary use of a more expensive solver. No feasible action is implied when every solver fails; retry/abort policy is not specified here.", "note": "The unresolved branch is an explicit reading aid, not a specified recovery algorithm. The reproduction can execute its best MPC candidate even when infeasible; RL fallback requires a checkpoint.", "implementations": [{"symbol": "Adaptive chunk loop", "path": "scripts/run_refine.py", "start": 351, "end": 434, "operation": "Plan over two chunks: try Replay → sample MPC corrections → optionally load an RL policy; execute only the current chunk.", "note": "RL requires --mode rl and a separately trained checkpoint. The loop can execute the best MPC candidate even if no sample is feasible, unlike the paper’s feasibility-gated description.", "key": "scripts/run_refine.py:351", "contract": "Controller state machine: optimize a 2H-step window, execute H steps, then repeat from the resulting physical state.", "docstring": null, "example": "For H=20 at t=40, candidate rollouts cover [40,80), but execution covers [40,60). A missing RL checkpoint leaves the MPC choice in place.", "steps": [{"start": 351, "end": 355, "code": "    t = f0\n    while t < f1:\n        t_end = min(t + Hn, f1)\n        t_win = min(t + 2 * Hn, f1)  # two-chunk lookahead window\n        crec = {\"t0\": t, \"t1\": t_end, \"window\": [t, t_win]}", "explanation": "Compute the current chunk end and the two-chunk lookahead end, clipping both at sequence length."}, {"start": 357, "end": 363, "code": "        # 1) Replay feasibility check over the two-chunk window\n        reset_envs_state(H, state[\"qpos\"], state[\"obj_T\"])\n        res = rollout(H, prep, t, t_win, args.substeps)\n        ok = feasible(res, Cp, CR)\n        crec[\"replay_feasible_window\"] = bool(ok[0].item())\n        ret_replay, _ = returns(res, C, args.c_contact, Cp, CR)\n        crec[\"replay_return\"] = float(ret_replay[0].item())", "explanation": "Reset to the saved boundary state, replay the reference through the entire lookahead, and record feasibility and return."}, {"start": 365, "end": 371, "code": "        chosen = None  # action deltas for the EXECUTED chunk\n        exec_policy = None  # residual policy for the EXECUTED chunk (rl mode)\n        if args.mode == \"replay\" or crec[\"replay_feasible_window\"]:\n            crec[\"mode\"] = \"replay\"\n        else:\n            # 2) MPC over the window (plan 2H, execute H)\n            crec[\"mode\"] = \"mpc\"", "explanation": "Keep Replay when it passes, or when replay-only mode is requested. Otherwise enter the MPC search."}, {"start": 380, "end": 389, "code": "            for it in range(args.mpc_iters):\n                sig = args.sigma0 * (0.5 ** it)\n                deltas = {s: (mu[s][0] + sig * scales[s][0] * torch.randn(num_envs, hlen, 3, device=dev),\n                              mu[s][1] + sig * scales[s][1] * torch.randn(num_envs, hlen, 12, device=dev))\n                          for s in SIDES}\n                reset_envs_state(H, state[\"qpos\"], state[\"obj_T\"])\n                res = rollout(H, prep, t, t_win, args.substeps, act_seqs=deltas,\n                              reward_cfg={\"contact_thresh\": args.contact_thresh},\n                              tool_verts=tool_verts)\n                J, _ = returns(res, C, args.c_contact, Cp, CR)", "explanation": "Sample wrist/finger corrections, reset to the same initial state, and score each candidate over the lookahead window."}, {"start": 390, "end": 401, "code": "                jmax = J.max()\n                if float(jmax) > best_J:\n                    best_J = float(jmax)\n                    bi = int(J.argmax())\n                    best_delta = {s: (deltas[s][0][bi], deltas[s][1][bi]) for s in SIDES}\n                n_feas = int(feasible(res, Cp, CR).sum())\n                max_feas = max(max_feas, n_feas)\n                # MPPI update\n                w = torch.softmax((J - jmax) / max(1e-3, 0.1 * float(J.std() + 1e-6)), dim=0)\n                for s in SIDES:\n                    mu[s] = ((w[:, None, None] * deltas[s][0]).sum(0, keepdim=True),\n                             (w[:, None, None] * deltas[s][1]).sum(0, keepdim=True))", "explanation": "Track the best sampled action sequence and update the sampling mean from softmax-weighted returns."}, {"start": 408, "end": 418, "code": "            # 2b) RL escalation: MPC found no feasible sample -> residual policy\n            if args.mode == \"rl\" and max_feas == 0:\n                from egoengine.rl.chunk_env import OBS_DIM, ACT_DIM\n                pol = load_chunk_policy(args.triplet, args.seq_id, t, t_end,\n                                        dev, OBS_DIM, ACT_DIM)\n                if pol is not None:\n                    crec[\"mode\"] = \"rl\"\n                    crec[\"rl_checkpoint\"] = \\\n                        f\"outputs/rl/{args.triplet}/{args.seq_id}/chunk_{t}_{t_end}\"\n                    exec_policy = pol\n                    chosen = None", "explanation": "Escalate only in rl mode and when MPC found no feasible sample. Load a chunk checkpoint; if one exists, replace the chosen MPC sequence with that residual policy."}, {"start": 424, "end": 434, "code": "        if chosen is not None:\n            exec_deltas = chosen  # (1,H,·) broadcast happens via expand below\n            # expand to all envs for batched stepping; only env 0 is recorded\n            exec_deltas = {s: (chosen[s][0].expand(H[\"num_envs\"], -1, -1),\n                               chosen[s][1].expand(H[\"num_envs\"], -1, -1)) for s in SIDES}\n        reset_envs_state(H, state[\"qpos\"], state[\"obj_T\"])\n        if exec_policy is not None:\n            res = rl_rollout(H, prep, t, t_end, args.substeps, exec_policy)\n        else:\n            res = rollout(H, prep, t, t_end, args.substeps, act_seqs=exec_deltas,\n                          record=True)", "explanation": "Reset again and execute only t…t_end, either using the residual policy or the chosen corrections. The next chunk is reconsidered rather than committed now."}]}]}, {"id": "S3.SS3.p1.1", "title": "Remove the human from the observation", "inputs": "RGB frame I and human arm/hand mask", "outputs": "Demonstrator-free background I_bg", "pseudo": "human_mask ← SAM2(frame, hand_keypoint_prompts)\nhuman_mask ← track_mask_through_video(human_mask)\nI_bg ← InpaintAnythingV2(I, human_mask)\npreserve the scene outside human_mask\nreturn I_bg for robot compositing", "explanation": "Locate the demonstrator’s arms and hands. Maintain the removal region through the clip. Fill the human region with plausible scene and object content. Retain the original task context outside the replacement region. Combine this background with the separately rendered visible robot.", "note": "The reproduction uses a LaMa inference wrapper and a crop/pad strategy for inpainting.", "implementations": [{"symbol": "hand_prompt_points() / segment_hands()", "path": "src/egoengine/visual/mask.py", "start": 33, "end": 95, "operation": "Project MANO keypoints into the image, prompt SAM2 on the first frame, and propagate the union of hand masks through the clip.", "note": "Implements hand masks; not the paper’s separate task-object tracking branch.", "key": "src/egoengine/visual/mask.py:33", "contract": "Inputs: sequence camera geometry, selected frame IDs and MANO hand joints. Output: a Boolean human-hand mask per requested frame.", "docstring": "Project each hand's MANO joints into `frame_idx`; keep in-image points.\n\nReturns {side: (points (M,2) float32 xy, labels (M,) int32 all-ones)}.\nSides with < 3 valid projected joints are dropped (hand not visible).", "example": "Two tracked hands become one removal mask via any(dim=0). The robot visibility mask is computed later by rendering, not by SAM2.", "steps": [{"start": 39, "end": 43, "code": "    prompts = {}\n    for side, joints in joints_by_side.items():\n        uv, z = seq.project(joints[PROMPT_JOINTS], frame_idx)\n        ok = (z > 0) & (uv[:, 0] >= 0) & (uv[:, 0] < seq.width) & \\\n             (uv[:, 1] >= 0) & (uv[:, 1] < seq.height)", "explanation": "Project selected 3D hand joints into image coordinates; reject points behind the camera or outside the frame."}, {"start": 44, "end": 46, "code": "        pts = uv[ok].astype(np.float32)\n        if pts.shape[0] >= 3:\n            prompts[side] = (pts, np.ones(pts.shape[0], dtype=np.int32))", "explanation": "Use surviving points as positive SAM2 prompts, but only if at least three are visible for that hand."}, {"start": 60, "end": 70, "code": "    # MANO joints at the prompt frame (first frame of the clip)\n    f0 = frames[0]\n    joints_by_side = {}\n    for side, hand in seq.hands.items():\n        h1 = dataclasses.replace(hand, pose=hand.pose[f0:f0 + 1],\n                                 trans=hand.trans[f0:f0 + 1],\n                                 frame_ids=hand.frame_ids[f0:f0 + 1])\n        _, joints, _ = mano_forward(h1)\n        joints_by_side[side] = joints[0]\n\n    prompts = hand_prompt_points(seq, f0, joints_by_side)", "explanation": "Reconstruct MANO joints at the clip’s first frame and derive image-space hand prompts from them."}, {"start": 71, "end": 72, "code": "    if not prompts:\n        raise RuntimeError(f\"no visible hand joints at frame {f0} — cannot prompt SAM2\")", "explanation": "Reject clips with no visible prompts instead of inventing a hand mask."}, {"start": 78, "end": 87, "code": "    tmp = tempfile.mkdtemp(prefix=\"sam2_frames_\")\n    for i, f in enumerate(frames):\n        img = seq.read_frame(f)\n        cv2.imwrite(str(Path(tmp) / f\"{i:06d}.jpg\"), img[:, :, ::-1])\n\n    predictor = build_sam2_video_predictor(CFG, str(CKPT), device=device)\n    masks: dict[int, np.ndarray] = {}\n    with torch.inference_mode(), torch.autocast(\"cuda\", dtype=torch.bfloat16):\n        state = predictor.init_state(video_path=tmp, offload_video_to_cpu=True,\n                                     offload_state_to_cpu=True)", "explanation": "Extract the selected RGB frames and initialize the SAM2 video predictor and tracking state."}, {"start": 89, "end": 91, "code": "        for obj_id, (side, (pts, labels)) in enumerate(prompts.items(), start=1):\n            predictor.add_new_points_or_box(state, frame_idx=0, obj_id=obj_id,\n                                            points=pts, labels=labels)", "explanation": "Assign a distinct object ID to each hand and register its positive point prompts on the first clip frame."}, {"start": 92, "end": 95, "code": "        for out_f, out_obj_ids, out_logits in predictor.propagate_in_video(state):\n            m = (out_logits > 0.0).any(dim=0).cpu().numpy().astype(bool)\n            m = np.squeeze(m)  # (n_obj,1,H,W) -> any over obj -> (1,H,W) -> (H,W)\n            masks[frames[out_f]] = m", "explanation": "Propagate through the video, threshold logits at zero, union all tracked hands, and map clip-relative indices back to the requested source frame IDs."}]}, {"symbol": "inpaint_frame()", "path": "src/egoengine/visual/inpaint.py", "start": 60, "end": 85, "operation": "Crop and pad the masked region, run LaMa on masked RGB plus mask, then preserve the original unmasked pixels.", "note": "Uses a LaMa inference wrapper as the human-removal counterpart.", "key": "src/egoengine/visual/inpaint.py:60", "contract": "Input: RGB uint8 image [H,W,3], Boolean removal mask [H,W], and a loaded LaMa model. Output: an RGB image with masked regions filled.", "docstring": "Inpaint `mask` region of an RGB uint8 frame. Returns RGB uint8.", "example": "A pixel outside the hand mask remains identical to the input even if LaMa changes it inside the larger crop. Only masked pixels are accepted from the prediction.", "steps": [{"start": 64, "end": 66, "code": "    H, W = rgb.shape[:2]\n    if not mask.any():\n        return rgb", "explanation": "Return the original frame immediately if nothing is masked."}, {"start": 67, "end": 70, "code": "    y0, y1, x0, x1 = _crop_around(mask, margin, H, W)\n    img = rgb[y0:y1, x0:x1].astype(np.float32) / 255.0\n    m = mask[y0:y1, x0:x1].astype(np.float32)\n    h, w = img.shape[:2]", "explanation": "Crop around the removal region with a margin, convert RGB to [0,1], and convert the mask to floating point."}, {"start": 71, "end": 73, "code": "    ph, pw = (8 - h % 8) % 8, (8 - w % 8) % 8\n    img = np.pad(img, ((0, ph), (0, pw), (0, 0)))\n    m = np.pad(m, ((0, ph), (0, pw)))", "explanation": "Pad height and width to multiples of eight to satisfy the model’s spatial requirements."}, {"start": 74, "end": 77, "code": "    with torch.inference_mode():\n        t_img = torch.from_numpy(img).permute(2, 0, 1)[None].to(device)\n        t_m = torch.from_numpy(m)[None, None].to(device)\n        out = model(torch.cat([t_img * (1 - t_m), t_m], dim=1))", "explanation": "Convert to channel-first tensors; zero masked RGB pixels and concatenate the mask as an extra channel before inference."}, {"start": 78, "end": 79, "code": "    out = out[0].permute(1, 2, 0).cpu().numpy()[:h, :w]\n    out = (np.clip(out, 0, 1) * 255).astype(np.uint8)", "explanation": "Remove padding from the model output, clip intensities to [0,1], and convert back to uint8."}, {"start": 80, "end": 85, "code": "    res = rgb.copy()\n    crop = res[y0:y1, x0:x1]\n    keep = ~mask[y0:y1, x0:x1]\n    out[keep] = crop[keep]\n    res[y0:y1, x0:x1] = out\n    return res", "explanation": "Copy original pixels back wherever the mask is false, then insert the repaired crop into the full frame."}]}]}, {"id": "S3.SS3.p2.1", "title": "Render an occlusion-aware robot mask", "inputs": "Robot trajectory, aligned camera and opaque object geometry", "outputs": "Visible-robot mask M", "pseudo": "place robot at q_robot[t] in the aligned scene\nI_transparent ← render(robot transparent, objects opaque)\nI_opaque ← render(robot opaque, objects opaque)\nM[pixel] ← RGB_difference(I_opaque, I_transparent) > threshold\nreturn I_opaque, M", "explanation": "Use the action branch’s trajectory in the same coordinates as the camera and objects. Keep the objects so occlusion is present even when the robot is invisible. Render the same scene with the robot visible. Pixels changed by the robot form its visible mask; occluded pixels remain unchanged. Use the mask to select the robot only where it is visible.", "note": "The reproduction adds morphological cleanup and small-component filtering after thresholding.", "implementations": [{"symbol": "EgoRenderer.render_frame() / visible_robot_mask()", "path": "src/egoengine/visual/render.py", "start": 125, "end": 154, "operation": "Render with and without the robot, threshold the maximum RGB-channel difference, then clean the visible-robot mask.", "note": "Adds morphology and small-component removal to the paper’s differential mask.", "key": "src/egoengine/visual/render.py:125", "contract": "Inputs: frame ID and left/right 19-value hand configurations. Outputs: robot scene RGB, background scene RGB and the visible robot mask.", "docstring": "Two-pass render. Returns (robot_rgb, bg_rgb, visible_robot_mask).", "example": "An object hiding the robot appears in both passes, so its pixels should have little difference. A visible robot pixel changes between passes and enters the mask.", "steps": [{"start": 129, "end": 130, "code": "        qw = {s: _qpos_to_world(q, self.T_base)\n              for s, q in ((\"left\", qpos_left19), (\"right\", qpos_right19))}", "explanation": "Transform each retargeted hand configuration from the pseudo-base frame into world coordinates."}, {"start": 131, "end": 133, "code": "        robot_rgb, _ = self.renderer.render(self._scene(frame_idx, True, qw))\n        bg_rgb, _ = self.renderer.render(self._scene(frame_idx, False, None))\n        mask = visible_robot_mask(robot_rgb, bg_rgb, diff_thresh)", "explanation": "Render with the robot and without it, keeping object geometry in the scene; compare the images to infer visible robot pixels."}, {"start": 143, "end": 144, "code": "    diff = np.abs(robot_rgb.astype(np.int16) - bg_rgb.astype(np.int16)).max(axis=2)\n    mask = (diff > thresh).astype(np.uint8)", "explanation": "Cast to signed integers to avoid uint8 subtraction wraparound. Take the maximum absolute channel difference and threshold it."}, {"start": 145, "end": 147, "code": "    k = cv2.getStructuringElement(cv2.MORPH_ELLIPSE, (5, 5))\n    mask = cv2.morphologyEx(mask, cv2.MORPH_OPEN, k)\n    mask = cv2.morphologyEx(mask, cv2.MORPH_CLOSE, k, iterations=2)", "explanation": "Apply morphological opening and closing to remove small noise and fill mask discontinuities."}, {"start": 149, "end": 154, "code": "    n, labels, stats, _ = cv2.connectedComponentsWithStats(mask, 8)\n    out = np.zeros_like(mask)\n    for i in range(1, n):\n        if stats[i, cv2.CC_STAT_AREA] >= 200:\n            out[labels == i] = 1\n    return out.astype(bool)", "explanation": "Keep connected components of at least 200 pixels; return the final Boolean mask."}]}]}, {"id": "S3.SS3.p2.2", "title": "Blend robot pixels into the repaired scene", "inputs": "Robot rendering I_robot, background I_bg, visible mask M", "outputs": "Synthesized robot observation I_obs", "pseudo": "for each image pixel p:\n    if M[p] is visible: I_obs[p] ← I_robot[p]\n    else: I_obs[p] ← I_bg[p]\nreturn I_obs", "explanation": "Apply the same selection independently at each image coordinate. Keep the rendered robot where the differential mask exposes it. Keep the human-removed scene where no robot is visible. Equivalent to I_obs = M·I_robot + (1−M)·I_bg.", "note": "", "implementations": [{"symbol": "composite()", "path": "src/egoengine/visual/blend.py", "start": 17, "end": 22, "operation": "np.where(mask, robot_rgb, inpainted) implements the visible-robot/background blend in Eq. 4.", "note": "", "key": "src/egoengine/visual/blend.py:17", "contract": "Inputs: two RGB uint8 images [H,W,3] and visible robot mask [H,W]. Output: the final synthesized robot observation.", "docstring": "Eq. 4 blend. All inputs (H, W, 3) uint8 / mask bool.", "example": "At mask=true, output[100,50]=robot_rgb[100,50]. At mask=false, that same coordinate is copied from inpainted. There is no alpha averaging in this implementation.", "steps": [{"start": 20, "end": 20, "code": "    m = vis_mask[..., None]", "explanation": "Add a singleton color-channel dimension to the mask, changing [H,W] to [H,W,1] so it broadcasts over RGB."}, {"start": 21, "end": 21, "code": "    out = np.where(m, robot_rgb, inpainted)", "explanation": "Select robot_rgb where the robot is visible; select the human-removed background everywhere else. This is the code counterpart of Eq. 4."}, {"start": 22, "end": 22, "code": "    return out.astype(np.uint8)", "explanation": "Return uint8 pixels, compatible with normal image storage and downstream preprocessing."}]}]}, {"id": "S3.SS4.p1.1", "title": "Pair generated demonstrations and train a policy", "inputs": "Visual observations, proprioception and refined actions", "outputs": "A robot visuomotor policy", "pseudo": "for each video and matching timestep t:\n    o[t] ← generated_robot_image[t], proprioception[t]\n    D.append(o[t], a_refined[t])\ntrain π on D using the paper’s learning objective\nduring control: observe → predict action → execute → observe again", "explanation": "Use the same instant for observations and the corresponding action. Combine the visual branch’s output with the robot’s internal state. Build the synthetic demonstration dataset across videos. Section 3.4 writes action regression; Appendix D describes a flow-matching action head. Use the next observation as feedback, rather than simply replaying training actions.", "note": "The reproduction follows Appendix D flow matching, not direct action MSE. Its training samples contain future action chunks.", "implementations": [{"symbol": "build_shard()", "path": "src/egoengine/policy/dataset.py", "start": 130, "end": 160, "operation": "Match generated images to refined trajectory frame IDs; pair actual proprioception with future action chunks and mark missing images invalid.", "note": "", "key": "src/egoengine/policy/dataset.py:130", "contract": "Input: a refined trajectory and generated frame files. Output: a compressed dataset shard with RGB, measured proprioception, future action chunks and validity flags.", "docstring": null, "example": "The image for source frame 40 must pair with proprioception at frame 40 and an action chunk beginning there. A missing image invalidates that row.", "steps": [{"start": 131, "end": 135, "code": "    traj_p = os.path.join(REFINE, triplet, seq, \"trajectory_refined.npz\")\n    traj = np.load(traj_p)\n    frames = traj[\"frames\"]\n    N = len(frames)\n    ql, qr = traj[\"qpos_left\"], traj[\"qpos_right\"]", "explanation": "Load refined data and preserve its frame ID array. Those IDs provide the alignment key for observations."}, {"start": 136, "end": 138, "code": "    proprio = np.concatenate(\n        [traj[\"qpos_left_actual\"], traj[\"qpos_right_actual\"]], axis=-1\n    ).astype(np.float32)", "explanation": "Concatenate actual left/right hand state into proprioception. Actual state can differ from the commanded trajectory under contact."}, {"start": 140, "end": 143, "code": "    act_l, val_l = build_actions(ql, HORIZON)\n    act_r, val_r = build_actions(qr, HORIZON)\n    actions = np.concatenate([act_l, act_r], axis=-1)            # (N,H,36)\n    valid = val_l & val_r", "explanation": "Convert left/right executed targets into future action chunks, concatenate their action dimensions, and require both horizons to be valid."}, {"start": 145, "end": 153, "code": "    images = np.zeros((N, IMG_SIZE, IMG_SIZE, 3), dtype=np.uint8)\n    img_source = np.full(N, -1, dtype=np.int8)\n    for i, fidx in enumerate(frames):\n        img, sid = load_frame(triplet, seq, int(fidx))\n        if img is None:\n            valid[i] = False\n        else:\n            images[i] = img\n            img_source[i] = sid", "explanation": "Load an image using each saved frame ID. If no image exists, invalidate that training sample instead of pairing an unrelated image."}, {"start": 155, "end": 160, "code": "    name = f\"{triplet.strip('()').replace(', ', '_')}__{seq}\"\n    os.makedirs(out_dir, exist_ok=True)\n    np.savez_compressed(\n        os.path.join(out_dir, f\"{name}.npz\"),\n        images=images, proprio=proprio, actions=actions,\n        img_source=img_source, valid=valid,", "explanation": "Write images, proprioception, actions, source labels and validity mask together, preserving the correspondence between rows."}]}, {"symbol": "HPTPolicy.compute_loss() / sample_action()", "path": "src/egoengine/policy/hpt_policy.py", "start": 160, "end": 175, "operation": "Encode RGB and normalized proprioception, train the conditioned action decoder, and denormalize sampled action chunks.", "note": "HPT-style reimplementation with Appendix D flow-matching loss, rather than a direct action-regression implementation of Eq. 5.", "key": "src/egoengine/policy/hpt_policy.py:160", "contract": "Inputs: RGB images, proprioceptive vectors and action targets. Training returns a scalar flow-matching loss; inference returns an action chunk in physical units.", "docstring": "Full policy: stems + fusion encoder + flow-matching action decoder.", "example": "A normalized predicted action value of 1 becomes mean+std in that coordinate. This wrapper’s loss is on decoder velocity, not a direct predicted-action MSE.", "steps": [{"start": 161, "end": 161, "code": "        proprio_n = (proprio - self.proprio_mean) / self.proprio_std", "explanation": "Standardize proprioception with dataset mean and standard deviation."}, {"start": 162, "end": 165, "code": "        tokens = torch.cat(\n            [self.visual_stem(images), self.proprio_stem(proprio_n)], dim=1\n        )\n        return self.encoder(tokens)", "explanation": "Extract visual and proprioceptive tokens, concatenate them, and compress them into context tokens through the fusion encoder."}, {"start": 167, "end": 169, "code": "    def compute_loss(self, images: torch.Tensor, proprio: torch.Tensor, actions: torch.Tensor) -> torch.Tensor:\n        actions_n = (actions - self.action_mean) / self.action_std\n        return self.decoder.compute_loss(actions_n, self.encode(images, proprio))", "explanation": "Normalize target actions and pass them with observation context to the decoder’s flow-matching training objective."}, {"start": 172, "end": 175, "code": "    def sample_action(self, images: torch.Tensor, proprio: torch.Tensor, steps: int = 10) -> torch.Tensor:\n        \"\"\"10-step Euler sampling -> UNNORMALIZED action chunk [B, H, A].\"\"\"\n        ctx = self.encode(images, proprio)\n        return self.decoder.sample(ctx, steps=steps) * self.action_std + self.action_mean", "explanation": "At inference, encode observations, sample an action chunk with the decoder, then undo action normalization."}]}]}, {"id": "A3.SS2.p2.1", "title": "Regularize toward human-retargeted motion", "inputs": "Current floating-hand pose/fingers and reference motion", "outputs": "Human-mimic penalty", "pseudo": "position_loss ← distance(base_position, reference_position)\nrotation_loss ← rotation_distance(base_rotation, reference_rotation)\njoint_loss ← distance(finger_joints, reference_joints)\npenalty ← weighted_sum(position_loss, rotation_loss, joint_loss)\nreward ← reward − penalty", "explanation": "Discourage translating the hand far from the retargeted prior. Keep hand orientation close to the reference. Penalize finger-configuration drift. Use the paper’s position, rotation and joint weights. Allow useful physical correction while charging for unnecessary deviation.", "note": "The reproduction subtracts squared quaternion-component differences, not a rotation-geodesic penalty.", "implementations": [{"symbol": "ChunkResidualEnv.step(): mimic term", "path": "src/egoengine/rl/chunk_env.py", "start": 335, "end": 346, "operation": "Optionally penalize squared differences between current base/finger state and retargeted reference.", "note": "Uses a quaternion-component penalty for base orientation; not the paper’s exact rotation metric.", "key": "src/egoengine/rl/chunk_env.py:335", "contract": "Optional human-mimic penalty: subtract a weighted squared discrepancy between current robot state and retargeted reference.", "docstring": "rsl_rl VecEnv: one chunk, residual PPO (see module docstring).", "example": "A lower tracking error can justify some deviation from the reference. This penalty makes deviation costly but does not forbid it; quaternion subtraction is not a geodesic angle.", "steps": [{"start": 335, "end": 337, "code": "        if self.use_mimic:\n            # pull current hand state toward the retargeted reference\n            obs_now = compute_obs(self.Hd, self.prep, t)", "explanation": "Only compute this regularizer when use_mimic is enabled; obtain the current observation after physics execution."}, {"start": 338, "end": 342, "code": "            m = 0.0\n            for i in range(2):\n                m = m + ((obs_now[:, i * HAND_OBS:i * HAND_OBS + 7]\n                          - obs_now[:, -2 * REF_OBS + i * REF_OBS:\n                                    -2 * REF_OBS + i * REF_OBS + 7]) ** 2).sum(-1)", "explanation": "For each hand, compare the current seven-value base pose with the corresponding reference pose. The seven values include position and quaternion components."}, {"start": 343, "end": 345, "code": "                m = m + ((obs_now[:, i * HAND_OBS + 7:i * HAND_OBS + 19]\n                          - obs_now[:, -2 * REF_OBS + i * REF_OBS + 7:\n                                    -2 * REF_OBS + i * REF_OBS + 19]) ** 2).sum(-1)", "explanation": "Add squared differences for the 12 finger coordinates."}, {"start": 346, "end": 346, "code": "            reward = reward - self.mimic_w * m", "explanation": "Subtract mimic_w times the total from reward, encouraging the policy to remain near the retargeted motion."}]}]}, {"id": "A3.SS2.p3.1", "title": "Penalize abrupt action changes", "inputs": "Current and previous action", "outputs": "Smoothness penalty", "pseudo": "Δa ← a[t] − a[t−1]\npenalty ← smoothness_weight × ‖Δa‖²\nreward ← reward − penalty", "explanation": "Measure the step-to-step change in the action command. Large jumps receive larger penalties. Encourage smoother control and more stable contact.", "note": "The reproduction applies this penalty to residual changes, rather than changes in the full command.", "implementations": [{"symbol": "ChunkResidualEnv.step(): smoothness term", "path": "src/egoengine/rl/chunk_env.py", "start": 347, "end": 349, "operation": "Optionally subtract the squared change in residual actions from the previous step.", "note": "Penalizes residual changes, not the full executed action difference.", "key": "src/egoengine/rl/chunk_env.py:347", "contract": "Optional smoothness penalty on the physical residual vector res, not on the full reference-plus-residual command.", "docstring": "rsl_rl VecEnv: one chunk, residual PPO (see module docstring).", "example": "If residuals stay zero while the reference changes abruptly, this penalty is zero. It smooths residual corrections rather than the entire executed motion.", "steps": [{"start": 347, "end": 347, "code": "        if self.use_smooth:", "explanation": "Enable the penalty only when use_smooth is set."}, {"start": 348, "end": 348, "code": "            reward = reward - self.smooth_w * ((res - self._prev_res) ** 2).sum(-1)", "explanation": "Subtract the weighted squared difference between this residual and the previous residual, summed across action coordinates."}, {"start": 349, "end": 349, "code": "        self._prev_res = res.clone()", "explanation": "Save a clone of the current residual for the next timestep’s comparison."}]}]}, {"id": "A3.SS2.p4.1", "title": "Reward opposition contact", "inputs": "Thumb contact flag and other-finger contacts", "outputs": "Binary contact bonus", "pseudo": "thumb_contact ← thumb touches manipulated object\nother_contact ← any non-thumb finger touches object\ngrasp ← thumb_contact AND other_contact\nbonus ← contact_weight if grasp else 0\nreward ← reward + bonus", "explanation": "Check whether the thumb participates in the grasp. At least one of the remaining fingers must contact the object. Both conditions must hold simultaneously in the paper’s contact reward. A sparse reward encourages a stable opposition-style grasp. Combine contact quality with the object-tracking objective.", "note": "Default reproduction behavior uses AND, but an optional contact_or variant uses OR.", "implementations": [{"symbol": "ChunkResidualEnv.step(): contact bonus", "path": "src/egoengine/rl/chunk_env.py", "start": 318, "end": 333, "operation": "Combine thumb/other-finger contact flags and add a binary contact reward.", "note": "Default uses AND; contact_or enables an OR variant. Reward masking approximates early termination while simulation continues.", "key": "src/egoengine/rl/chunk_env.py:318", "contract": "Per-step reward: sum object error, compute contact bonus, then mask by episode validity unless dense-after-violation mode is enabled.", "docstring": "rsl_rl VecEnv: one chunk, residual PPO (see module docstring).", "example": "With ct=true and co=false, default grasp=false but contact_or gives true. Reward masking does not stop physics; the environment continues to the chunk timeout.", "steps": [{"start": 319, "end": 325, "code": "        e_t = torch.zeros(self.num_envs, device=self.device)\n        viol = torch.zeros(self.num_envs, dtype=torch.bool, device=self.device)\n        for r in (\"tool\", \"target\"):\n            ts = get_object_poses_batched(self.Hd, r)\n            dp, dR = pose_error_batched(ts, self.prep[\"objects\"][r][\"gt\"][t])\n            e_t += torch.sqrt(dp ** 2 + dR ** 2)\n            viol |= (dp > self.Cp) | (dR > self.CR)", "explanation": "Initialize error/violation vectors, evaluate tool and target, accumulate sqrt(dp²+dR²), and flag either threshold crossing."}, {"start": 326, "end": 327, "code": "        ct, co = self.contact_fn(self.Hd)\n        grasp = (ct | co) if self.contact_or else (ct & co)", "explanation": "Read thumb and other-finger contact flags. Default AND rewards opposition contact; the optional OR mode rewards either contact."}, {"start": 328, "end": 328, "code": "        r_obj = self.C - e_t + self.c_contact * grasp.float()", "explanation": "Subtract tracking error from C and add the binary grasp bonus."}, {"start": 329, "end": 332, "code": "        if self.dense_after_violation:\n            reward = r_obj\n        else:\n            reward = r_obj * self._alive.float()", "explanation": "Dense mode keeps rewarding after a violation; otherwise multiply by the previous alive state."}, {"start": 333, "end": 333, "code": "        self._alive &= ~viol  # early-termination semantics: 0 after violation", "explanation": "Update alive after calculating reward. Unlike returns(), this ordering retains reward at the first violating step and suppresses subsequent rewards."}, {"start": 335, "end": 346, "code": "        if self.use_mimic:\n            # pull current hand state toward the retargeted reference\n            obs_now = compute_obs(self.Hd, self.prep, t)\n            m = 0.0\n            for i in range(2):\n                m = m + ((obs_now[:, i * HAND_OBS:i * HAND_OBS + 7]\n                          - obs_now[:, -2 * REF_OBS + i * REF_OBS:\n                                    -2 * REF_OBS + i * REF_OBS + 7]) ** 2).sum(-1)\n                m = m + ((obs_now[:, i * HAND_OBS + 7:i * HAND_OBS + 19]\n                          - obs_now[:, -2 * REF_OBS + i * REF_OBS + 7:\n                                    -2 * REF_OBS + i * REF_OBS + 19]) ** 2).sum(-1)\n            reward = reward - self.mimic_w * m", "explanation": "Optional mimic regularization subtracts current/reference hand pose and finger discrepancies."}, {"start": 347, "end": 349, "code": "        if self.use_smooth:\n            reward = reward - self.smooth_w * ((res - self._prev_res) ** 2).sum(-1)\n        self._prev_res = res.clone()", "explanation": "Optional smoothness regularization subtracts consecutive residual differences. These are the last reward terms: there is no height/lifting bonus here."}]}]}, {"id": "A3.SS2.p5.1", "title": "Reward lifting when required", "inputs": "Object height z[t] and initial height z[0]", "outputs": "Task-dependent lifting bonus", "pseudo": "if task requires lifting:\n    height_gain ← z[t] − z[0]\n    bonus ← lifting_reward(height_gain; paper coefficients)\n    reward ← reward + bonus", "explanation": "Only add lifting encouragement for tasks that need upward object motion. Compare current object height with its initial height. Use the clipping/weighting specified in the adjacent equation. Add lifting to the other optimization reward terms.", "note": "Missing in the reproduction: the linked reward assembly has tracking, contact, mimic and smoothness terms, but no lifting term.", "implementations": [{"symbol": "ChunkResidualEnv.step(): reward assembly", "path": "src/egoengine/rl/chunk_env.py", "start": 318, "end": 349, "operation": "Inspect the implemented tracking, contact, mimic and smoothness reward terms.", "note": "Missing counterpart: no explicit lifting reward is implemented in this reward assembly.", "key": "src/egoengine/rl/chunk_env.py:318", "contract": "Per-step reward: sum object error, compute contact bonus, then mask by episode validity unless dense-after-violation mode is enabled.", "docstring": "rsl_rl VecEnv: one chunk, residual PPO (see module docstring).", "example": "With ct=true and co=false, default grasp=false but contact_or gives true. Reward masking does not stop physics; the environment continues to the chunk timeout.", "steps": [{"start": 319, "end": 325, "code": "        e_t = torch.zeros(self.num_envs, device=self.device)\n        viol = torch.zeros(self.num_envs, dtype=torch.bool, device=self.device)\n        for r in (\"tool\", \"target\"):\n            ts = get_object_poses_batched(self.Hd, r)\n            dp, dR = pose_error_batched(ts, self.prep[\"objects\"][r][\"gt\"][t])\n            e_t += torch.sqrt(dp ** 2 + dR ** 2)\n            viol |= (dp > self.Cp) | (dR > self.CR)", "explanation": "Initialize error/violation vectors, evaluate tool and target, accumulate sqrt(dp²+dR²), and flag either threshold crossing."}, {"start": 326, "end": 327, "code": "        ct, co = self.contact_fn(self.Hd)\n        grasp = (ct | co) if self.contact_or else (ct & co)", "explanation": "Read thumb and other-finger contact flags. Default AND rewards opposition contact; the optional OR mode rewards either contact."}, {"start": 328, "end": 328, "code": "        r_obj = self.C - e_t + self.c_contact * grasp.float()", "explanation": "Subtract tracking error from C and add the binary grasp bonus."}, {"start": 329, "end": 332, "code": "        if self.dense_after_violation:\n            reward = r_obj\n        else:\n            reward = r_obj * self._alive.float()", "explanation": "Dense mode keeps rewarding after a violation; otherwise multiply by the previous alive state."}, {"start": 333, "end": 333, "code": "        self._alive &= ~viol  # early-termination semantics: 0 after violation", "explanation": "Update alive after calculating reward. Unlike returns(), this ordering retains reward at the first violating step and suppresses subsequent rewards."}, {"start": 335, "end": 346, "code": "        if self.use_mimic:\n            # pull current hand state toward the retargeted reference\n            obs_now = compute_obs(self.Hd, self.prep, t)\n            m = 0.0\n            for i in range(2):\n                m = m + ((obs_now[:, i * HAND_OBS:i * HAND_OBS + 7]\n                          - obs_now[:, -2 * REF_OBS + i * REF_OBS:\n                                    -2 * REF_OBS + i * REF_OBS + 7]) ** 2).sum(-1)\n                m = m + ((obs_now[:, i * HAND_OBS + 7:i * HAND_OBS + 19]\n                          - obs_now[:, -2 * REF_OBS + i * REF_OBS + 7:\n                                    -2 * REF_OBS + i * REF_OBS + 19]) ** 2).sum(-1)\n            reward = reward - self.mimic_w * m", "explanation": "Optional mimic regularization subtracts current/reference hand pose and finger discrepancies."}, {"start": 347, "end": 349, "code": "        if self.use_smooth:\n            reward = reward - self.smooth_w * ((res - self._prev_res) ** 2).sum(-1)\n        self._prev_res = res.clone()", "explanation": "Optional smoothness regularization subtracts consecutive residual differences. These are the last reward terms: there is no height/lifting bonus here."}]}]}, {"id": "A4.SS1.p1.1", "title": "Map observations to an action chunk", "inputs": "RGB image and proprioceptive state", "outputs": "Predicted action sequence", "pseudo": "V ← visual_stem(image)\nP ← proprioceptive_stem(normalize(state))\ncontext ← fusion_encoder(concat(V, P))\nactions ← flow_matching_head(context)\nreturn actions", "explanation": "Extract visual observation tokens. Represent the normalized internal robot state as tokens. Combine modalities into compact context tokens. Condition the action decoder on the observation context. Use the action prediction in downstream closed-loop control.", "note": "", "implementations": [{"symbol": "HPTPolicy.compute_loss() / sample_action()", "path": "src/egoengine/policy/hpt_policy.py", "start": 160, "end": 175, "operation": "Encode RGB and normalized proprioception, train the conditioned action decoder, and denormalize sampled action chunks.", "note": "HPT-style reimplementation with Appendix D flow-matching loss, rather than a direct action-regression implementation of Eq. 5.", "key": "src/egoengine/policy/hpt_policy.py:160", "contract": "Inputs: RGB images, proprioceptive vectors and action targets. Training returns a scalar flow-matching loss; inference returns an action chunk in physical units.", "docstring": "Full policy: stems + fusion encoder + flow-matching action decoder.", "example": "A normalized predicted action value of 1 becomes mean+std in that coordinate. This wrapper’s loss is on decoder velocity, not a direct predicted-action MSE.", "steps": [{"start": 161, "end": 161, "code": "        proprio_n = (proprio - self.proprio_mean) / self.proprio_std", "explanation": "Standardize proprioception with dataset mean and standard deviation."}, {"start": 162, "end": 165, "code": "        tokens = torch.cat(\n            [self.visual_stem(images), self.proprio_stem(proprio_n)], dim=1\n        )\n        return self.encoder(tokens)", "explanation": "Extract visual and proprioceptive tokens, concatenate them, and compress them into context tokens through the fusion encoder."}, {"start": 167, "end": 169, "code": "    def compute_loss(self, images: torch.Tensor, proprio: torch.Tensor, actions: torch.Tensor) -> torch.Tensor:\n        actions_n = (actions - self.action_mean) / self.action_std\n        return self.decoder.compute_loss(actions_n, self.encode(images, proprio))", "explanation": "Normalize target actions and pass them with observation context to the decoder’s flow-matching training objective."}, {"start": 172, "end": 175, "code": "    def sample_action(self, images: torch.Tensor, proprio: torch.Tensor, steps: int = 10) -> torch.Tensor:\n        \"\"\"10-step Euler sampling -> UNNORMALIZED action chunk [B, H, A].\"\"\"\n        ctx = self.encode(images, proprio)\n        return self.decoder.sample(ctx, steps=steps) * self.action_std + self.action_mean", "explanation": "At inference, encode observations, sample an action chunk with the decoder, then undo action normalization."}]}]}, {"id": "A4.SS1.p2.1", "title": "Turn RGB into visual tokens", "inputs": "RGB image", "outputs": "Fixed-length visual token sequence", "pseudo": "image ← ImageNet_normalize(image)\nfeatures ← ResNet18_without_global_pool(image)\ntokens ← linear_project(flatten_spatial(features))\nvisual_tokens ← cross_attention(learned_queries, tokens)\nreturn visual_tokens", "explanation": "Apply channel-wise ImageNet mean and standard deviation. Keep spatial locations instead of pooling them into a single vector. Map each spatial location into transformer hidden dimension. Use a fixed number of learned queries to summarize image evidence. Feed the visual tokens into the multimodal encoder.", "note": "", "implementations": [{"symbol": "VisualStem", "path": "src/egoengine/policy/hpt_policy.py", "start": 58, "end": 92, "operation": "Normalize RGB, extract ResNet-18 spatial features, project them, and use learned queries to extract visual tokens.", "note": "", "key": "src/egoengine/policy/hpt_policy.py:58", "contract": "Input: RGB tensors [B,3,224,224] in [0,1]. Output: a fixed number of visual tokens [B,n_tokens,D].", "docstring": "ResNet-18 truncated before GAP -> query-token extraction (App D.1).", "example": "A 224×224 image becomes 49 spatial tokens, then defaults to 16 visual query tokens. Spatial features survive because global average pooling is disabled.", "steps": [{"start": 65, "end": 67, "code": "        self.backbone = timm.create_model(\n            \"resnet18\", pretrained=False, num_classes=0, global_pool=\"\"\n        )  # forward -> [B, 512, 7, 7] at 224px", "explanation": "Create ResNet-18 without classification or global pooling, retaining its spatial feature map."}, {"start": 69, "end": 81, "code": "            # timm 1.0 fetches resnet18.tv_in1k from HF (unreachable here);\n            # the identical torchvision checkpoint is in the torch-hub cache.\n            ckpt = os.path.join(\n                os.path.expanduser(os.environ.get(\"TORCH_HOME\", \"~/.cache/torch\")),\n                \"hub\", \"checkpoints\", \"resnet18-f37072fd.pth\",\n            )\n            if os.path.exists(ckpt):\n                sd = torch.load(ckpt, map_location=\"cpu\", weights_only=True)\n                sd = {k: v for k, v in sd.items() if not k.startswith(\"fc.\")}\n                missing, unexpected = self.backbone.load_state_dict(sd, strict=False)\n                assert not unexpected and all(\"fc.\" in k for k in missing), (missing, unexpected)\n            else:\n                print(f\"[hpt_policy] WARNING: {ckpt} not found — visual stem is RANDOM\")", "explanation": "When pretrained is requested, try the locally cached ImageNet checkpoint and omit its classifier weights. If missing, the backbone remains random and a warning is printed."}, {"start": 82, "end": 85, "code": "        self.proj = nn.Linear(512, embed_dim)\n        self.extract = CrossAttnExtract(embed_dim, n_tokens, heads)\n        self.register_buffer(\"px_mean\", IMAGENET_MEAN.view(1, 3, 1, 1), persistent=False)\n        self.register_buffer(\"px_std\", IMAGENET_STD.view(1, 3, 1, 1), persistent=False)", "explanation": "Project 512 feature channels into transformer dimension D; create learned-query extraction and ImageNet normalization buffers."}, {"start": 89, "end": 90, "code": "        x = (images - self.px_mean) / self.px_std  # ImageNet normalization (App D.1)\n        fmap = self.backbone(x)                     # [B,512,7,7]", "explanation": "Normalize each RGB channel, then obtain a [B,512,7,7] spatial feature map for a 224×224 image."}, {"start": 91, "end": 91, "code": "        tokens = self.proj(fmap.flatten(2).transpose(1, 2))  # [B,49,D]", "explanation": "Flatten the 7×7 grid into 49 locations, transpose to [B,49,512], and project each location to dimension D."}, {"start": 92, "end": 92, "code": "        return self.extract(tokens)", "explanation": "Cross-attention compresses the variable spatial evidence into n_tokens learned-query outputs."}]}]}, {"id": "A4.SS1.p3.1", "title": "Turn proprioception into tokens", "inputs": "Joint positions and end-effector state", "outputs": "Fixed-length proprioceptive tokens", "pseudo": "state ← normalize(proprioception)\nstate_token ← linear_project(state)\ntokens ← cross_attention(learned_queries, state_token)\nreturn tokens", "explanation": "Standardize the internal observation before projection. Map robot-state coordinates to transformer hidden dimension. Use learned queries to produce a fixed-length token representation. Combine these tokens with the visual tokens in the next stage.", "note": "", "implementations": [{"symbol": "ProprioStem", "path": "src/egoengine/policy/hpt_policy.py", "start": 95, "end": 105, "operation": "Linearly project proprioception and extract learned-query tokens; normalization occurs in HPTPolicy.encode().", "note": "", "key": "src/egoengine/policy/hpt_policy.py:95", "contract": "Input: normalized robot state [B,proprio_dim]. Output: learned-query proprioceptive tokens [B,n_tokens,D].", "docstring": "Linear projection + query-token extraction (App D.1).", "example": "Normalization occurs before this module, in HPTPolicy.encode(). The [:,None,:] operation changes [B,D] into [B,1,D]; it does not add a time history.", "steps": [{"start": 101, "end": 101, "code": "        self.extract = CrossAttnExtract(embed_dim, n_tokens, heads)", "explanation": "Project each robot-state vector into the shared transformer dimension."}, {"start": 102, "end": 102, "code": "", "explanation": "Create a learned-query cross-attention extractor with a fixed output token count."}, {"start": 104, "end": 105, "code": "        return self.extract(self.proj(proprio)[:, None, :])\n", "explanation": "Insert a length-one sequence axis into the projected state and extract tokens from that single state token."}]}]}, {"id": "A4.SS1.p4.1", "title": "Fuse observation tokens", "inputs": "Visual and proprioceptive tokens", "outputs": "Compact context tokens", "pseudo": "tokens ← concat(visual_tokens, proprioceptive_tokens)\nsequence ← concat(learned_context_tokens, tokens)\nencoded ← transformer_encoder(sequence)\ncontext ← encoded[context_token_positions]\nreturn context", "explanation": "Concatenate stems along the token-sequence axis. Add a small set of trainable summary tokens. Let context and observation tokens interact through attention. Retain only the compact context portion to condition action generation. The decoder attends to these context tokens.", "note": "", "implementations": [{"symbol": "FusionEncoder", "path": "src/egoengine/policy/hpt_policy.py", "start": 107, "end": 125, "operation": "Prepend learned context tokens to the concatenated observation tokens, run the transformer, and retain the context outputs.", "note": "", "key": "src/egoengine/policy/hpt_policy.py:107", "contract": "Input: concatenated observation tokens [B,N,D]. Output: a compact context sequence [B,n_ctx,D].", "docstring": "Prepend context tokens, transformer encoder, read out context tokens.", "example": "With 16 visual and 8 proprioceptive tokens, N=24. Prepending 8 context tokens produces 32 transformer positions; only 8 are returned to condition the decoder.", "steps": [{"start": 112, "end": 112, "code": "        self.ctx_tokens = nn.Parameter(torch.zeros(1, n_ctx, embed_dim))", "explanation": "Allocate trainable context tokens shared across observations."}, {"start": 113, "end": 118, "code": "        layer = nn.TransformerEncoderLayer(\n            embed_dim, heads, 4 * embed_dim, dropout=0.0,\n            activation=\"gelu\", batch_first=True, norm_first=True,\n        )\n        self.encoder = nn.TransformerEncoder(layer, num_blocks)\n        self.n_ctx = n_ctx", "explanation": "Build a stack of pre-norm transformer layers with GELU feed-forward blocks and four-times-D hidden width."}, {"start": 119, "end": 119, "code": "        nn.init.normal_(self.ctx_tokens, std=0.02)", "explanation": "Initialize context tokens with small random values so they can learn distinct summaries."}, {"start": 123, "end": 123, "code": "        x = torch.cat([self.ctx_tokens.expand(tokens.shape[0], -1, -1), tokens], dim=1)", "explanation": "Expand the context tokens across the batch and prepend them to the observation sequence."}, {"start": 124, "end": 124, "code": "        return self.encoder(x)[:, : self.n_ctx]", "explanation": "Run self-attention over both context and observation tokens, then keep only the first n_ctx positions."}]}]}, {"id": "A4.SS2.p1.1", "title": "Predict action-space velocity", "inputs": "Noisy action chunk a_τ, time τ, observation context", "outputs": "Velocity v_θ(a_τ, τ, context)", "pseudo": "tokens ← action_embedding(a_τ) + position_embedding\ntokens ← tokens + time_embedding(τ)\nfor each decoder block:\n    tokens ← self_attention(tokens)\n    tokens ← cross_attention(tokens, context)\nvelocity ← linear_output(tokens)", "explanation": "Represent each future action timestep separately. τ is interpolation time; it is not the future-action timestep. Refine the action-token representation. Exchange information within the predicted action chunk. Condition action generation on fused observation tokens. Map hidden states back to action-space coordinates.", "note": "", "implementations": [{"symbol": "FlowMatchingDecoder.forward()", "path": "src/egoengine/policy/fm_decoder.py", "start": 100, "end": 109, "operation": "Embed the noisy action chunk and interpolation time, attend to observation context, and project to action-space velocity.", "note": "", "key": "src/egoengine/policy/fm_decoder.py:100", "contract": "Inputs: noisy actions [B,T,A], interpolation time [B], context [B,N,D]. Output: velocity predictions [B,T,A].", "docstring": "x_tau [B,T,A], tau [B] in (0,1], context [B,N,D] -> velocity [B,T,A].", "example": "T describes future control timesteps; τ describes progress from noise to clean actions. They are different axes, even though both are sometimes called “time”.", "steps": [{"start": 102, "end": 103, "code": "        B, T, _ = x_tau.shape\n        assert T == self.horizon, f\"expected horizon {self.horizon}, got {T}\"", "explanation": "Read batch/horizon/action dimensions and require the configured action horizon."}, {"start": 104, "end": 104, "code": "        h = self.action_in(x_tau) + self.pos_emb", "explanation": "Project each noisy action into model width and add a learned positional embedding to distinguish action timesteps."}, {"start": 105, "end": 106, "code": "        t_emb = self.time_mlp(sinusoidal_embedding(tau.float(), self.embed_dim))\n        h = h + t_emb[:, None, :]", "explanation": "Embed the continuous interpolation time, map it through an MLP, and broadcast it to every action token."}, {"start": 107, "end": 108, "code": "        for blk in self.blocks:\n            h = blk(h, context)", "explanation": "Apply decoder blocks so action tokens interact with one another and attend to observation context."}, {"start": 109, "end": 109, "code": "        return self.action_out(self.ln_out(h))", "explanation": "Normalize hidden states and project back to action coordinates. These outputs are velocities along the noise interpolation, not final actions."}]}]}, {"id": "A4.SS2.p3.1", "title": "Predict action-space velocity", "inputs": "Noisy action chunk a_τ, time τ, observation context", "outputs": "Velocity v_θ(a_τ, τ, context)", "pseudo": "tokens ← action_embedding(a_τ) + position_embedding\ntokens ← tokens + time_embedding(τ)\nfor each decoder block:\n    tokens ← self_attention(tokens)\n    tokens ← cross_attention(tokens, context)\nvelocity ← linear_output(tokens)", "explanation": "Represent each future action timestep separately. τ is interpolation time; it is not the future-action timestep. Refine the action-token representation. Exchange information within the predicted action chunk. Condition action generation on fused observation tokens. Map hidden states back to action-space coordinates.", "note": "", "implementations": [{"symbol": "FlowMatchingDecoder.forward()", "path": "src/egoengine/policy/fm_decoder.py", "start": 100, "end": 109, "operation": "Embed the noisy action chunk and interpolation time, attend to observation context, and project to action-space velocity.", "note": "", "key": "src/egoengine/policy/fm_decoder.py:100", "contract": "Inputs: noisy actions [B,T,A], interpolation time [B], context [B,N,D]. Output: velocity predictions [B,T,A].", "docstring": "x_tau [B,T,A], tau [B] in (0,1], context [B,N,D] -> velocity [B,T,A].", "example": "T describes future control timesteps; τ describes progress from noise to clean actions. They are different axes, even though both are sometimes called “time”.", "steps": [{"start": 102, "end": 103, "code": "        B, T, _ = x_tau.shape\n        assert T == self.horizon, f\"expected horizon {self.horizon}, got {T}\"", "explanation": "Read batch/horizon/action dimensions and require the configured action horizon."}, {"start": 104, "end": 104, "code": "        h = self.action_in(x_tau) + self.pos_emb", "explanation": "Project each noisy action into model width and add a learned positional embedding to distinguish action timesteps."}, {"start": 105, "end": 106, "code": "        t_emb = self.time_mlp(sinusoidal_embedding(tau.float(), self.embed_dim))\n        h = h + t_emb[:, None, :]", "explanation": "Embed the continuous interpolation time, map it through an MLP, and broadcast it to every action token."}, {"start": 107, "end": 108, "code": "        for blk in self.blocks:\n            h = blk(h, context)", "explanation": "Apply decoder blocks so action tokens interact with one another and attend to observation context."}, {"start": 109, "end": 109, "code": "        return self.action_out(self.ln_out(h))", "explanation": "Normalize hidden states and project back to action coordinates. These outputs are velocities along the noise interpolation, not final actions."}]}]}, {"id": "A4.SS2.p2.1", "title": "Train the flow-matching action head", "inputs": "Clean action chunk a₁ and observation context", "outputs": "Mean-squared velocity loss", "pseudo": "a₀ ∼ Normal(0, I)\nτ ∼ Uniform(0, 1)\na_τ ← τ a₀ + (1−τ) a₁\nv_target ← a₀ − a₁\nv_pred ← decoder(a_τ, τ, context)\nloss ← mean_squared_error(v_pred, v_target)", "explanation": "Draw Gaussian noise with the same shape as the action chunk. Use a continuous random point on the noise-to-action path. τ=1 is noise and τ=0 is clean action under this convention. Differentiate the interpolation with respect to τ. Condition the learned vector field on observation and time. Train velocity prediction rather than directly regressing the clean action.", "note": "", "implementations": [{"symbol": "FlowMatchingDecoder.compute_loss()", "path": "src/egoengine/policy/fm_decoder.py", "start": 111, "end": 119, "operation": "Sample noise and time τ; interpolate xτ = τ·noise + (1−τ)·actions; regress velocity toward noise − actions with MSE.", "note": "", "key": "src/egoengine/policy/fm_decoder.py:111", "contract": "Training contract: clean normalized actions [B,T,A] plus observation context → scalar mean-squared velocity error.", "docstring": "Flow-matching L2 loss. actions = clean chunk a1 [B,T,A].", "example": "For one scalar action=2, noise=−1 and τ=0.25, xτ=1.25 and target velocity=−3. The model learns this direction, rather than predicting 2 directly.", "steps": [{"start": 113, "end": 114, "code": "        B = actions.shape[0]\n        a0 = torch.randn_like(actions)", "explanation": "Read the batch size and draw Gaussian noise with exactly the same shape as the clean action chunk."}, {"start": 115, "end": 115, "code": "        tau = torch.rand(B, device=actions.device, dtype=actions.dtype).clamp_min(1e-4)", "explanation": "Draw one interpolation time per example; clamp away from zero."}, {"start": 116, "end": 116, "code": "        x_tau = tau[:, None, None] * a0 + (1.0 - tau[:, None, None]) * actions", "explanation": "Broadcast τ over horizon and action coordinates. τ=1 gives noise; τ=0 gives the clean action sequence."}, {"start": 117, "end": 117, "code": "        target_v = a0 - actions  # dx/dtau, paper Eq. D.1", "explanation": "Differentiate xτ=τ·noise+(1−τ)·actions with respect to τ: the target velocity is noise−actions."}, {"start": 118, "end": 118, "code": "        pred_v = self.forward(x_tau, tau, context)", "explanation": "Predict that velocity from the interpolated actions, time and observation context."}, {"start": 119, "end": 119, "code": "        return F.mse_loss(pred_v, target_v)", "explanation": "Average squared velocity error over batch, horizon and action dimensions for backpropagation."}]}]}, {"id": "A4.SS2.p4.1", "title": "Generate actions by backward Euler integration", "inputs": "Observation context and 10 inference steps", "outputs": "Action chunk at τ=0", "pseudo": "a ∼ Normal(0, I)\nΔτ ← 1 / 10\nfor τ in [1.0, 0.9, …, 0.1]:\n    v ← decoder(a, τ, context)\n    a ← a − Δτ v\nreturn a", "explanation": "Initialize the action sequence at the noise end of the path. Ten fixed steps cover the interval from one to zero. Evaluate the field at decreasing interpolation times. Use the current action estimate and observation context. Subtract velocity because integration proceeds in the decreasing-τ direction. The final state approximates clean actions at τ=0.", "note": "", "implementations": [{"symbol": "FlowMatchingDecoder.sample()", "path": "src/egoengine/policy/fm_decoder.py", "start": 122, "end": 132, "operation": "Initialize Gaussian action noise and apply 10 backward Euler updates: x ← x − Δt·v(x, τ, context).", "note": "", "key": "src/egoengine/policy/fm_decoder.py:122", "contract": "Inference contract: observation context → a normalized action chunk. Start at noise (τ=1) and integrate toward data (τ=0).", "docstring": "10-step Euler integration tau=1 -> 0 (paper App D.2). Returns [B,T,A].", "example": "If the current scalar is −1 and the velocity is −3, one step gives −1−0.1×(−3)=−0.7: it moves toward the clean action, not farther into noise.", "steps": [{"start": 124, "end": 127, "code": "        B = context.shape[0]\n        x = torch.randn(\n            B, self.horizon, self.action_dim, device=context.device, dtype=context.dtype\n        )", "explanation": "Draw an initial Gaussian chunk with the configured horizon and action dimension for each observation."}, {"start": 128, "end": 128, "code": "        dt = 1.0 / steps", "explanation": "Set the Euler step size to 1/steps. The default 10 steps gives Δτ=0.1."}, {"start": 129, "end": 130, "code": "        for i in range(steps):\n            tau = torch.full((B,), 1.0 - i * dt, device=context.device, dtype=context.dtype)", "explanation": "Evaluate the vector field at τ=1.0,0.9,…,0.1 for ten steps."}, {"start": 131, "end": 131, "code": "            x = x - dt * self.forward(x, tau, context)", "explanation": "Subtract Δτ times the predicted velocity because integration moves toward decreasing τ."}, {"start": 132, "end": 132, "code": "        return x", "explanation": "Return the final denoised action chunk. HPTPolicy.sample_action() subsequently restores physical units."}]}]}, {"id": "code-sam2", "title": "Load SAM2 and propagate hand masks", "inputs": "SAM2 config/checkpoint, clip JPEGs, projected MANO prompts", "outputs": "Boolean hand mask per frame", "pseudo": "predictor = load_video_predictor(config, checkpoint)\nstate = predictor.init_state(clip)\nfor hand in visible_hands:\n    add_positive_prompts(state, hand)\nfor frame, logits in propagate(state):\n    mask[frame] = union(logits > 0)", "note": "Unofficial implementation masks hands from MANO prompts; it does not recover full arm masks or object tracking in this function.", "implementations": [{"symbol": "Load SAM2 and propagate hand masks", "path": "src/egoengine/visual/mask.py", "start": 83, "end": 96, "operation": "Load the SAM2 video model once, initialize clip state, add prompts, and propagate masks.", "note": "", "contract": "Load the SAM2 video model once, initialize clip state, add prompts, and propagate masks.", "steps": [{"start": 83, "end": 96, "code": "    predictor = build_sam2_video_predictor(CFG, str(CKPT), device=device)\n    masks: dict[int, np.ndarray] = {}\n    with torch.inference_mode(), torch.autocast(\"cuda\", dtype=torch.bfloat16):\n        state = predictor.init_state(video_path=tmp, offload_video_to_cpu=True,\n                                     offload_state_to_cpu=True)\n        predictor.reset_state(state)\n        for obj_id, (side, (pts, labels)) in enumerate(prompts.items(), start=1):\n            predictor.add_new_points_or_box(state, frame_idx=0, obj_id=obj_id,\n                                            points=pts, labels=labels)\n        for out_f, out_obj_ids, out_logits in predictor.propagate_in_video(state):\n            m = (out_logits > 0.0).any(dim=0).cpu().numpy().astype(bool)\n            m = np.squeeze(m)  # (n_obj,1,H,W) -> any over obj -> (1,H,W) -> (H,W)\n            masks[frames[out_f]] = m\n            if verbose and out_f % 10 == 0:", "explanation": "Load the SAM2 video model once, initialize clip state, add prompts, and propagate masks."}]}]}, {"id": "code-mano", "title": "Load MANO and decode the hand representation", "inputs": "Hand pose [N,48], translation [N,3], shape betas [10]", "outputs": "Vertices [N,778,3], joints [N,21,3], triangle faces", "pseudo": "layer = load_MANO(side, use_pca=False)\nvertices, joints = layer(global_orientation, articulation, betas)\nvertices, joints = align_wrist_and_translate_to_world(...)", "note": "MANO is a parametric hand model, not a generic object mesh format; this repository uses smplx and TACO’s full 45-dimensional articulation convention.", "implementations": [{"symbol": "Load MANO and decode the hand representation", "path": "src/egoengine/taco/mano.py", "start": 140, "end": 157, "operation": "Cache one MANO layer per side/model root/device and load the model with full articulation rather than PCA coefficients.", "note": "", "contract": "Cache one MANO layer per side/model root/device and load the model with full articulation rather than PCA coefficients.", "steps": [{"start": 140, "end": 157, "code": "def get_mano_layer(side: str, mano_root: str = DEFAULT_MANO_ROOT, device: str = \"cpu\"):\n    \"\"\"Cached smplx MANO layer for one side ('left'/'right').\"\"\"\n    key = (side, mano_root, device)\n    if key not in _layer_cache:\n        _install_chumpy_stub()\n        import smplx\n        import torch\n        layer = smplx.create(\n            model_path=mano_root,\n            model_type=\"mano\",\n            is_rhand=(side == \"right\"),\n            use_pca=False,\n            flat_hand_mean=True,\n            batch_size=1,\n        )\n        layer = layer.to(device)\n        _layer_cache[key] = layer\n    return _layer_cache[key]", "explanation": "Cache one MANO layer per side/model root/device and load the model with full articulation rather than PCA coefficients."}]}]}, {"id": "code-assets", "title": "Asset formats and coordinate conventions", "inputs": "TACO .pkl, .npy, .txt, .obj, and RGB .mp4 files", "outputs": "Synchronized hand, object, and camera records", "pseudo": "hand.pose:      float32[N,48]  # axis-angle 3 + articulation 45\nhand.trans:     float32[N,3]   # meters, world frame\nhand.betas:     float32[10]\nobject.poses:   float32[N,4,4] # object → world, meters\ncamera.K:       float32[3,3]\ncamera.T:       float32[N,4,4] # world → camera\nmesh.vertices *= 0.01         # OBJ centimeters → meters", "note": "These are the reproduction’s TACO conventions, not a universal asset schema. A mesh provides geometry; per-frame pose matrices place it in the world.", "implementations": [{"symbol": "Asset formats and coordinate conventions", "path": "src/egoengine/taco/loader.py", "start": 49, "end": 80, "operation": "Typed records preserve array shape, units, and transform direction across the pipeline.", "note": "", "contract": "Typed records preserve array shape, units, and transform direction across the pipeline.", "steps": [{"start": 49, "end": 80, "code": "    trans: np.ndarray       # (N, 3) float32, meters, world frame\n    betas: np.ndarray       # (10,) float32\n    frame_ids: np.ndarray   # (N,) int, 0-based array index of each entry\n\n\n@dataclass\nclass ObjectData:\n    \"\"\"One manipulated object (tool or target).\"\"\"\n    role: str               # \"tool\" | \"target\"\n    object_id: str          # e.g. \"071\"\n    name: str               # category from the triplet string (e.g. \"brush\")\n    poses: np.ndarray       # (N, 4, 4) object-to-world, meters\n    mesh_path: str          # OBJ, vertices in CENTIMETERS -> scale by 0.01\n\n\n@dataclass\nclass TacoSequence:\n    triplet: str\n    seq_id: str\n    root: str\n    n_frames: int\n    fps: float\n    width: int\n    height: int\n    video_path: str\n    intrinsic: np.ndarray               # (3, 3)\n    extrinsics: np.ndarray              # (N, 4, 4) world-to-camera, OpenCV\n    hands: dict = field(default_factory=dict)     # \"left\"/\"right\" -> HandData\n    objects: dict = field(default_factory=dict)   # \"tool\"/\"target\" -> ObjectData\n\n    # ------------------------------------------------------------------ API\n    def read_frame(self, idx: int) -> np.ndarray:", "explanation": "Typed records preserve array shape, units, and transform direction across the pipeline."}]}]}, {"id": "solver-replay", "title": "Replay · execute the Mink reference", "inputs": "Retargeted wrist/finger commands and current simulator state", "outputs": "Reference rollout and object-tracking feasibility", "pseudo": "for each control step t in the window:\n    action ← reference_command[t]\n    state ← simulate(state, action)\n    error ← object_tracking_error(state, target[t])\n    if error exceeds threshold: mark rollout infeasible\nreturn rollout, feasibility", "explanation": "", "note": "Mink constructs the kinematic reference; Replay executes it without optimization. The reproduction uses the shared rollout loop with act_seqs=None.", "implementations": [{"symbol": "rollout()", "path": "scripts/run_refine.py", "start": 124, "end": 149, "operation": "Apply reference wrist/finger targets plus optional deltas, step physics, and measure the resulting object poses.", "note": "", "key": "scripts/run_refine.py:124", "contract": "Rollout loop: N parallel simulator environments over a frame window. Optional corrections have shapes [N,H,3] for wrist translation and [N,H,12] for fingers.", "docstring": "Roll all envs from frame t0 to t1 (exclusive). act_seqs: optional\n{side: (wrist_dxyz (N,H,3)T, q12_d (N,H,12)T)} deltas on the reference.\nReturns dict with per-frame per-env dp/dR (N,H) per role, contact flags,\nand env-0 executed/actual states if record.", "example": "With act_seqs=None this is Replay. Supplying sampled deltas reuses the same simulator loop to evaluate MPC candidates.", "steps": [{"start": 124, "end": 127, "code": "    prev = None\n    for k in range(hlen):\n        t = t0 + k\n        ra = ref_actions(prep, t)", "explanation": "Start a frame window with no previous command and retrieve the retargeted reference action at each frame."}, {"start": 129, "end": 133, "code": "        for s in SIDES:\n            p = torch.tensor(ra[s][0], dtype=torch.float32, device=dev)[None].expand(n, 3)\n            q = torch.tensor(ra[s][1], dtype=torch.float32, device=dev)[None].expand(n, 4)\n            v = torch.tensor(ra[s][2], dtype=torch.float32, device=dev)[None].expand(n, 3)\n            q12 = torch.tensor(ra[s][3], dtype=torch.float32, device=dev)[None].expand(n, 12)", "explanation": "Broadcast the reference wrist position, quaternion, velocity and 12 finger targets to all candidate environments."}, {"start": 134, "end": 137, "code": "            if act_seqs is not None:\n                p = p + act_seqs[s][0][:, k]\n                q12 = q12 + act_seqs[s][1][:, k]\n            acts[s] = (p, q, v, q12)", "explanation": "If candidate corrections exist, add them to wrist position and finger targets. This MPC path does not change the wrist quaternion."}, {"start": 138, "end": 142, "code": "        for sub in range(1, substeps + 1):\n            if prev is not None:\n                apply_action_frame(H, acts, prev, sub / substeps)\n            else:\n                apply_action_frame(H, acts, acts, 1.0)", "explanation": "Interpolate from the previous action over physics substeps; for the first frame, apply the current action directly."}, {"start": 143, "end": 145, "code": "            H[\"physx\"].simulate(H[\"dt\"], 0.0)\n            H[\"physx\"].fetch_results()\n        prev = acts", "explanation": "Advance PhysX and fetch results before reading state. These calls make the reference into a physical rollout rather than just a pose sequence."}, {"start": 146, "end": 149, "code": "        for r in roles:\n            ts = get_object_poses_batched(H, r)\n            dp[r][:, k], dR[r][:, k] = pose_error_batched(\n                ts, prep[\"objects\"][r][\"gt\"][t])", "explanation": "Read each simulated object pose and compare it with the demonstration at the same frame; store translation and rotation error for feasibility and scoring."}]}, {"symbol": "Scheduler · REPLAY", "path": "scripts/run_refine.py", "start": 351, "end": 434, "operation": "Plan over two chunks: try Replay → sample MPC corrections → optionally load an RL policy; execute only the current chunk.", "note": "RL requires --mode rl and a separately trained checkpoint. The loop can execute the best MPC candidate even if no sample is feasible, unlike the paper’s feasibility-gated description.", "key": "scripts/run_refine.py:351", "contract": "Controller state machine: optimize a 2H-step window, execute H steps, then repeat from the resulting physical state.", "docstring": null, "example": "For H=20 at t=40, candidate rollouts cover [40,80), but execution covers [40,60). A missing RL checkpoint leaves the MPC choice in place.", "steps": [{"start": 357, "end": 363, "code": "        # 1) Replay feasibility check over the two-chunk window\n        reset_envs_state(H, state[\"qpos\"], state[\"obj_T\"])\n        res = rollout(H, prep, t, t_win, args.substeps)\n        ok = feasible(res, Cp, CR)\n        crec[\"replay_feasible_window\"] = bool(ok[0].item())\n        ret_replay, _ = returns(res, C, args.c_contact, Cp, CR)\n        crec[\"replay_return\"] = float(ret_replay[0].item())", "explanation": "Reset to the saved boundary state, replay the reference through the entire lookahead, and record feasibility and return."}, {"start": 365, "end": 371, "code": "        chosen = None  # action deltas for the EXECUTED chunk\n        exec_policy = None  # residual policy for the EXECUTED chunk (rl mode)\n        if args.mode == \"replay\" or crec[\"replay_feasible_window\"]:\n            crec[\"mode\"] = \"replay\"\n        else:\n            # 2) MPC over the window (plan 2H, execute H)\n            crec[\"mode\"] = \"mpc\"", "explanation": "Keep Replay when it passes, or when replay-only mode is requested. Otherwise enter the MPC search."}]}]}, {"id": "solver-mpc", "title": "MPC · sampled trajectory refinement (SPIDER baseline)", "inputs": "Current state, reference commands, object targets and search budget", "outputs": "Locally refined action sequence", "pseudo": "mean_delta ← 0\nrepeat for the search budget:\n    deltas ← sample corrections around mean_delta\n    rollouts ← simulate(reference + deltas, from same boundary state)\n    scores ← object_tracking_returns(rollouts)\n    mean_delta ← weighted_average(deltas, softmax(scores / temperature))\n    retain best candidate\nexecute current chunk; replan at next boundary", "explanation": "", "note": "The paper compares against SPIDER. These excerpts are the unofficial reproduction’s MPPI-style MPC, not the original SPIDER implementation. It perturbs wrist translation and finger targets; its best candidate may be executed even if infeasible.", "implementations": [{"symbol": "MPC sampling loop", "path": "scripts/run_refine.py", "start": 370, "end": 406, "operation": "Sample Gaussian wrist/finger corrections, score simulated rollouts, update the mean with softmax return weights, and retain the best sample.", "note": "", "key": "scripts/run_refine.py:370", "contract": "MPC searches wrist/finger correction sequences over the two-chunk window. Each simulator environment evaluates one sampled candidate.", "docstring": null, "example": "A high-return candidate can still violate the boundary late in the window. The best-return selection and feasible-sample count are separate quantities in this code.", "steps": [{"start": 372, "end": 377, "code": "            hlen = t_win - t\n            mu = {s: (torch.zeros(num_envs, hlen, 3, device=dev),\n                      torch.zeros(num_envs, hlen, 12, device=dev)) for s in SIDES}\n            scales = {s: (torch.full((1, 1, 3), SIGMA_WRIST, device=dev),\n                          torch.full((1, 1, 12), SIGMA_FINGER, device=dev))\n                      for s in SIDES}", "explanation": "Set the window length, initialize correction means to zero, and choose separate physical noise scales for wrist and finger coordinates."}, {"start": 380, "end": 384, "code": "            for it in range(args.mpc_iters):\n                sig = args.sigma0 * (0.5 ** it)\n                deltas = {s: (mu[s][0] + sig * scales[s][0] * torch.randn(num_envs, hlen, 3, device=dev),\n                              mu[s][1] + sig * scales[s][1] * torch.randn(num_envs, hlen, 12, device=dev))\n                          for s in SIDES}", "explanation": "At each search iteration halve sigma and draw Gaussian perturbations around the current mean. The reference remains the center of the initial search."}, {"start": 385, "end": 389, "code": "                reset_envs_state(H, state[\"qpos\"], state[\"obj_T\"])\n                res = rollout(H, prep, t, t_win, args.substeps, act_seqs=deltas,\n                              reward_cfg={\"contact_thresh\": args.contact_thresh},\n                              tool_verts=tool_verts)\n                J, _ = returns(res, C, args.c_contact, Cp, CR)", "explanation": "Reset every candidate to the identical chunk-start state, simulate its corrections, and compute the return; this makes candidate scores comparable."}, {"start": 390, "end": 394, "code": "                jmax = J.max()\n                if float(jmax) > best_J:\n                    best_J = float(jmax)\n                    bi = int(J.argmax())\n                    best_delta = {s: (deltas[s][0][bi], deltas[s][1][bi]) for s in SIDES}", "explanation": "Keep the highest-return sampled sequence found so far. Selection here maximizes return rather than explicitly restricting the choice to feasible samples."}, {"start": 395, "end": 396, "code": "                n_feas = int(feasible(res, Cp, CR).sum())\n                max_feas = max(max_feas, n_feas)", "explanation": "Count feasible candidates separately, retaining whether any search iteration found one."}, {"start": 398, "end": 401, "code": "                w = torch.softmax((J - jmax) / max(1e-3, 0.1 * float(J.std() + 1e-6)), dim=0)\n                for s in SIDES:\n                    mu[s] = ((w[:, None, None] * deltas[s][0]).sum(0, keepdim=True),\n                             (w[:, None, None] * deltas[s][1]).sum(0, keepdim=True))", "explanation": "Convert returns to softmax weights and take a weighted mean of sampled corrections. This MPPI-style update steers later samples toward better rollouts."}, {"start": 404, "end": 406, "code": "            crec[\"mpc_best_return\"] = best_J\n            crec[\"mpc_max_feasible_samples\"] = max_feas\n            chosen = {s: (best_delta[s][0][None], best_delta[s][1][None]) for s in SIDES}", "explanation": "Record search diagnostics and keep the best correction sequence for execution."}]}, {"symbol": "Scheduler · MPC", "path": "scripts/run_refine.py", "start": 351, "end": 434, "operation": "Plan over two chunks: try Replay → sample MPC corrections → optionally load an RL policy; execute only the current chunk.", "note": "RL requires --mode rl and a separately trained checkpoint. The loop can execute the best MPC candidate even if no sample is feasible, unlike the paper’s feasibility-gated description.", "key": "scripts/run_refine.py:351", "contract": "Controller state machine: optimize a 2H-step window, execute H steps, then repeat from the resulting physical state.", "docstring": null, "example": "For H=20 at t=40, candidate rollouts cover [40,80), but execution covers [40,60). A missing RL checkpoint leaves the MPC choice in place.", "steps": [{"start": 380, "end": 389, "code": "            for it in range(args.mpc_iters):\n                sig = args.sigma0 * (0.5 ** it)\n                deltas = {s: (mu[s][0] + sig * scales[s][0] * torch.randn(num_envs, hlen, 3, device=dev),\n                              mu[s][1] + sig * scales[s][1] * torch.randn(num_envs, hlen, 12, device=dev))\n                          for s in SIDES}\n                reset_envs_state(H, state[\"qpos\"], state[\"obj_T\"])\n                res = rollout(H, prep, t, t_win, args.substeps, act_seqs=deltas,\n                              reward_cfg={\"contact_thresh\": args.contact_thresh},\n                              tool_verts=tool_verts)\n                J, _ = returns(res, C, args.c_contact, Cp, CR)", "explanation": "Sample wrist/finger corrections, reset to the same initial state, and score each candidate over the lookahead window."}, {"start": 390, "end": 401, "code": "                jmax = J.max()\n                if float(jmax) > best_J:\n                    best_J = float(jmax)\n                    bi = int(J.argmax())\n                    best_delta = {s: (deltas[s][0][bi], deltas[s][1][bi]) for s in SIDES}\n                n_feas = int(feasible(res, Cp, CR).sum())\n                max_feas = max(max_feas, n_feas)\n                # MPPI update\n                w = torch.softmax((J - jmax) / max(1e-3, 0.1 * float(J.std() + 1e-6)), dim=0)\n                for s in SIDES:\n                    mu[s] = ((w[:, None, None] * deltas[s][0]).sum(0, keepdim=True),\n                             (w[:, None, None] * deltas[s][1]).sum(0, keepdim=True))", "explanation": "Track the best sampled action sequence and update the sampling mean from softmax-weighted returns."}, {"start": 424, "end": 434, "code": "        if chosen is not None:\n            exec_deltas = chosen  # (1,H,·) broadcast happens via expand below\n            # expand to all envs for batched stepping; only env 0 is recorded\n            exec_deltas = {s: (chosen[s][0].expand(H[\"num_envs\"], -1, -1),\n                               chosen[s][1].expand(H[\"num_envs\"], -1, -1)) for s in SIDES}\n        reset_envs_state(H, state[\"qpos\"], state[\"obj_T\"])\n        if exec_policy is not None:\n            res = rl_rollout(H, prep, t, t_end, args.substeps, exec_policy)\n        else:\n            res = rollout(H, prep, t, t_end, args.substeps, act_seqs=exec_deltas,\n                          record=True)", "explanation": "Reset again and execute only t…t_end, either using the residual policy or the chosen corrections. The next chunk is reconsidered rather than committed now."}]}]}, {"id": "solver-rl", "title": "RL · residual policy with PPO (H2S2R baseline)", "inputs": "Hand state, current/reference object poses and reference commands", "outputs": "Learned action corrections for a difficult chunk", "pseudo": "training:\n    observation ← hand_state + object_poses + reference_command\n    residual ← policy(observation)\n    action ← compose(reference_command, residual)\n    rollout in simulation; compute rewards\n    update actor and critic with PPO\nexecution:\n    load trained chunk checkpoint\n    apply residual policy at each control step", "explanation": "", "note": "The paper cites H2S2R as its RL baseline. This is the unofficial reproduction’s residual PPO implementation, not original H2S2R code. Training runs separately; adaptive escalation loads an existing chunk checkpoint and does not automatically train a new policy.", "implementations": [{"symbol": "residual_to_actions() / compute_obs()", "path": "src/egoengine/rl/chunk_env.py", "start": 99, "end": 158, "operation": "Concatenate hand/object/reference state; add translation and finger residuals and compose the rotation residual with the reference quaternion.", "note": "", "key": "src/egoengine/rl/chunk_env.py:99", "contract": "Residual action: [N,36], with 18 values per hand (3 translation, 3 rotation-vector, 12 fingers). Observation: hand state, current/reference object poses and retargeted hand commands.", "docstring": "a_t = a_base + δa_t. residual: (N,36) already in PHYSICAL units\n(multiply normalized policy output by ACT_SCALES first).", "example": "A +0.01 m x residual shifts the wrist target by 1 cm. Rotation residuals cannot simply be added to quaternion components; they are composed as rotations.", "steps": [{"start": 103, "end": 108, "code": "    base = ref_base_actions(prep, t, n, dev)\n    acts = {}\n    for i, s in enumerate(SIDES):\n        sl = slice(i * HAND_ACT, (i + 1) * HAND_ACT)\n        dxyz, drot, dfing = residual[:, sl][:,:3], residual[:, sl][:,3:6], residual[:, sl][:,6:]\n        p, q, v, q12 = base[s]", "explanation": "Retrieve the base action, slice the policy output into left/right hands, and split each into translation, rotation and finger corrections."}, {"start": 109, "end": 111, "code": "        q_new = _quat_mul_wxyz(_rotvec_to_quat_wxyz(drot), q)\n        q_new = q_new / q_new.norm(dim=-1, keepdim=True).clamp(min=1e-9)\n        acts[s] = (p + dxyz, q_new, v, q12 + dfing)", "explanation": "Convert the rotation vector to a quaternion and compose it with the reference orientation. Normalize it; add the other residuals to position and finger targets."}, {"start": 130, "end": 132, "code": "    n = H[\"num_envs\"]\n    origins = torch.arange(n, dtype=torch.float32, device=dev)[:, None] * \\\n        torch.tensor([H[\"spacing\"], 0.0, 0.0], device=dev)", "explanation": "Compute environment origins so observations can remove the spatial offsets used to separate parallel simulations."}, {"start": 134, "end": 145, "code": "    for s in SIDES:\n        av = H[\"art\"][s]\n        lt = wp.to_torch(av.get_link_transforms()).clone().to(dev)\n        lv = wp.to_torch(av.get_link_velocities()).clone().to(dev)\n        qp = wp.to_torch(av.get_dof_positions()).clone().to(dev)\n        qv = wp.to_torch(av.get_dof_velocities()).clone().to(dev)\n        root_p = lt[:, 0, :3] - origins\n        rq = lt[:, 0, 3:7]  # xyzw -> wxyz\n        root_q = torch.stack([rq[:, 3], rq[:, 0], rq[:, 1], rq[:, 2]], dim=-1)\n        q12 = qp[:, H[\"inv_joint_idx\"][s]]\n        qd12 = qv[:, H[\"inv_joint_idx\"][s]]\n        parts += [root_p, root_q, q12, lv[:, 0, :6], qd12]", "explanation": "Read hand link poses/velocities and finger positions/velocities. Reorder the root quaternion from xyzw to wxyz before concatenating the hand state."}, {"start": 147, "end": 153, "code": "    for r in (\"tool\", \"target\"):\n        ts = get_object_poses_batched(H, r)  # (N,7) xyz + quat xyzw, local frame\n        gt = prep[\"objects\"][r][\"gt\"][t]\n        p_ref = torch.tensor(gt[:3, 3], dtype=torch.float32, device=dev)[None].expand(n, 3)\n        q_ref = torch.tensor(Rotation.from_matrix(gt[:3, :3]).as_quat(),\n                             dtype=torch.float32, device=dev)[None].expand(n, 4)\n        parts += [ts[:, :3], ts[:, 3:7], p_ref, q_ref]", "explanation": "Append both the current simulated and desired object poses. The residual policy can therefore observe deviation from the demonstration."}, {"start": 154, "end": 158, "code": "    for s in SIDES:\n        q = torch.tensor(prep[\"qpos\"][s][t], dtype=torch.float32, device=dev)\n        parts += [q[:3][None].expand(n, 3), q[3:7][None].expand(n, 4),\n                  q[7:][None].expand(n, 12)]\n    return torch.cat(parts, dim=-1)", "explanation": "Append reference hand commands and concatenate the observation parts along the feature dimension."}]}, {"symbol": "OnPolicyRunner / runner.learn()", "path": "scripts/train_residual.py", "start": 280, "end": 318, "operation": "Configure rsl_rl PPO and train the residual policy for one failed chunk.", "note": "", "key": "scripts/train_residual.py:280", "contract": "Training setup for one failed chunk: an ActorCritic residual policy optimized by the external rsl_rl PPO implementation.", "docstring": null, "example": "The adaptive controller loads checkpoints produced by this training script. It does not launch this PPO training automatically at each escalation.", "steps": [{"start": 283, "end": 288, "code": "            \"class_name\": \"ActorCritic\",\n            \"init_noise_std\": 0.6,\n            \"actor_hidden_dims\": [256, 256],\n            \"critic_hidden_dims\": [256, 256],\n            \"activation\": \"elu\",\n        },", "explanation": "Use two 256-unit hidden layers for actor and critic with ELU activation; initialize policy noise with standard deviation 0.6."}, {"start": 290, "end": 295, "code": "            \"class_name\": \"PPO\",\n            \"value_loss_coef\": 1.0,\n            \"use_clipped_value_loss\": True,\n            \"clip_param\": 0.2,\n            \"entropy_coef\": 0.002,\n            \"num_learning_epochs\": 4,", "explanation": "Select PPO, clipped value loss, policy clipping 0.2, and an entropy bonus. The actual PPO update lives inside rsl_rl, not this script."}, {"start": 296, "end": 303, "code": "            \"num_mini_batches\": 4,\n            \"learning_rate\": 3.0e-4,\n            \"schedule\": \"adaptive\",\n            \"gamma\": 0.99,\n            \"lam\": 0.95,\n            \"desired_kl\": 0.02,\n            \"max_grad_norm\": 1.0,\n        },", "explanation": "Configure epochs/minibatches, learning rate, discount γ, advantage parameter λ, KL target and gradient clipping."}, {"start": 306, "end": 307, "code": "    from rsl_rl.runners import OnPolicyRunner\n    runner = OnPolicyRunner(env, train_cfg, log_dir=str(log_dir), device=H[\"device\"])", "explanation": "Bind the prepared simulation environment and configuration to OnPolicyRunner, which collects rollouts and performs updates."}, {"start": 310, "end": 312, "code": "    t_learn = time.time()\n    it_done = 0\n    while it_done < args.iters and (time.time() - t_learn) < args.max_minutes * 60:", "explanation": "Stop when either the iteration budget or wall-clock budget is exhausted."}, {"start": 313, "end": 318, "code": "        if cur_on:\n            # anneal from start-scale to 1.0 over the first half of --iters\n            f = min(1.0, it_done / max(1, 0.5 * args.iters))\n            s = args.curriculum_start_scale + (1.0 - args.curriculum_start_scale) * f\n            env.set_thresholds(Cp * s, CR * s)\n        runner.learn(num_learning_iterations=1, init_at_random_ep_len=False)", "explanation": "If curriculum is enabled, anneal feasibility thresholds toward the target values; then perform one learning iteration."}]}, {"symbol": "Scheduler · RL", "path": "scripts/run_refine.py", "start": 351, "end": 434, "operation": "Plan over two chunks: try Replay → sample MPC corrections → optionally load an RL policy; execute only the current chunk.", "note": "RL requires --mode rl and a separately trained checkpoint. The loop can execute the best MPC candidate even if no sample is feasible, unlike the paper’s feasibility-gated description.", "key": "scripts/run_refine.py:351", "contract": "Controller state machine: optimize a 2H-step window, execute H steps, then repeat from the resulting physical state.", "docstring": null, "example": "For H=20 at t=40, candidate rollouts cover [40,80), but execution covers [40,60). A missing RL checkpoint leaves the MPC choice in place.", "steps": [{"start": 408, "end": 418, "code": "            # 2b) RL escalation: MPC found no feasible sample -> residual policy\n            if args.mode == \"rl\" and max_feas == 0:\n                from egoengine.rl.chunk_env import OBS_DIM, ACT_DIM\n                pol = load_chunk_policy(args.triplet, args.seq_id, t, t_end,\n                                        dev, OBS_DIM, ACT_DIM)\n                if pol is not None:\n                    crec[\"mode\"] = \"rl\"\n                    crec[\"rl_checkpoint\"] = \\\n                        f\"outputs/rl/{args.triplet}/{args.seq_id}/chunk_{t}_{t_end}\"\n                    exec_policy = pol\n                    chosen = None", "explanation": "Escalate only in rl mode and when MPC found no feasible sample. Load a chunk checkpoint; if one exists, replace the chosen MPC sequence with that residual policy."}, {"start": 424, "end": 434, "code": "        if chosen is not None:\n            exec_deltas = chosen  # (1,H,·) broadcast happens via expand below\n            # expand to all envs for batched stepping; only env 0 is recorded\n            exec_deltas = {s: (chosen[s][0].expand(H[\"num_envs\"], -1, -1),\n                               chosen[s][1].expand(H[\"num_envs\"], -1, -1)) for s in SIDES}\n        reset_envs_state(H, state[\"qpos\"], state[\"obj_T\"])\n        if exec_policy is not None:\n            res = rl_rollout(H, prep, t, t_end, args.substeps, exec_policy)\n        else:\n            res = rollout(H, prep, t, t_end, args.substeps, act_seqs=exec_deltas,\n                          record=True)", "explanation": "Reset again and execute only t…t_end, either using the residual policy or the chosen corrections. The next chunk is reconsidered rather than committed now."}]}]}]};
 const panel=document.createElement('aside');panel.id='reader-code';panel.hidden=true;panel.setAttribute('aria-label','Unofficial reproduction code');
 panel.innerHTML='<div class="glossary-heading"><h2>Code · unofficial</h2><button aria-label="Hide code">×</button></div><p>Independent reproduction, not official author code.</p><a class="repo-source" target="_blank" rel="noopener">Repository ↗</a><label for="code-topic">Method or component</label><input id="code-search" type="search" aria-label="Search methods and components" placeholder="Search HPT, MPC, SAM2…"><div class="code-shortcuts"><button data-code-solver="replay">Replay</button><button data-code-solver="mpc">MPC</button><button data-code-solver="rl">RL</button><button id="code-hpt">HPT · policy architecture</button></div><select id="code-topic"></select><p id="code-search-status" role="status" hidden></p><button id="code-follow">Follow passage</button><div id="code-content"></div>';
 document.querySelector('.layout').append(panel);
 const toggle=document.createElement('button');toggle.id='code-toggle';toggle.textContent='Code · unofficial';toggle.setAttribute('aria-controls',panel.id);document.getElementById('glossary-toggle').after(toggle);
 const select=panel.querySelector('select'),content=panel.querySelector('#code-content');let visible=false,follow=true,lastBlock=null;
 panel.querySelector('.repo-source').href=data.repo;panel.querySelector('.repo-source').textContent='egoengine-repro · '+data.commit.slice(0,7)+' ↗';
 function setVisible(value){if(value)window.setSummaryVisible?.(false);visible=value;panel.hidden=!value;document.body.classList.toggle('code-open',value);toggle.setAttribute('aria-expanded',String(value));if(value)window.setGlossaryVisible?.(false);try{localStorage.setItem('egoengine-code-visible',String(value))}catch{}}
 window.setCodeVisible=setVisible;toggle.onclick=()=>setVisible(!visible);panel.querySelector('.glossary-heading button').onclick=()=>setVisible(false);
 const make=(tag,text)=>{const el=document.createElement(tag);el.textContent=text;return el};
 const groups=[
  ['1 · Scene reconstruction',c=>/Reconstruct the digital twin|pseudo robot base/.test(c.title)],
  ['2 · Models & asset formats',c=>c.id.startsWith('code-')],
  ['3 · Action generation → retargeting & solvers',c=>c.id.startsWith('solver-')||c.id.startsWith('S3.SS2')||c.id==='S3.F2'||c.id.startsWith('A3.SS1')],
  ['4 · Action generation → rewards & constraints',c=>c.id.startsWith('A3.SS2')],
  ['5 · Visual generation → remove, render, blend',c=>c.id.startsWith('S3.SS3')],
  ['6 · HPT policy → dataset & observation encoder',c=>c.id.startsWith('S3.SS4')||c.id.startsWith('A4.SS1')],
  ['7 · HPT policy → flow-matching action decoder',c=>c.id.startsWith('A4.SS2')]
 ];
 const groupFor=card=>groups.find(([,matches])=>matches(card))?.[0]||'Other components';
 const location=card=>card.id.startsWith('solver-')?'solver guide':card.id.startsWith('code-')?'repo guide':card.id==='S3.F2'?'Fig. 2':card.id.replace(/^S/,'§').replace(/^A1/,'App. A').replace(/^A3/,'App. C').replace(/^A4/,'App. D').replace(/\.SSS?/g,'.').replace(/\.p(\d+)\.(\d+)$/, ' · passage $1.$2');
 const topicTitle=card=>card.id==='A4.SS1.p1.1'?'HPT architecture overview · observations → actions':card.id==='S3.SS4.p1.1'?'HPT training dataset · paired demonstrations':card.title;
 const search=panel.querySelector('#code-search'),status=panel.querySelector('#code-search-status');
 function populate(){const chosen=content.dataset.card||data.cards[0].id,query=search.value.trim().toLowerCase().replace(/\bhpc\b/g,'hpt');select.replaceChildren();let count=0;
  for(const [name,matches] of groups){const group=document.createElement('optgroup');group.label=name;
   for(const card of data.cards.filter(c=>groupFor(c)===name).sort((a,b)=>Number(b.id.startsWith('solver-'))-Number(a.id.startsWith('solver-')))){const keywords=[name,topicTitle(card),card.title,card.inputs,card.outputs,card.pseudo].join(' ').toLowerCase();if(query&&!query.split(/\s+/).every(word=>keywords.includes(word)))continue;const option=make('option',topicTitle(card)+' — '+location(card));option.value=card.id;group.append(option);count++}if(group.children.length)select.append(group)
  }select.value=chosen;select.disabled=count===0;status.hidden=!query;status.textContent=count?count+' matching topics':'No matching components.';
 }
 search.oninput=populate;
 panel.querySelectorAll('[data-code-solver]').forEach(button=>button.onclick=()=>{follow=false;search.value='';populate();render('solver-'+button.dataset.codeSolver)});
 panel.querySelector('#code-hpt').onclick=()=>{follow=false;search.value='';populate();render('A4.SS1.p1.1')};
 populate();
 function render(id){const card=data.cards.find(c=>c.id===id)||data.cards[0];if(content.dataset.card===card.id)return;content.dataset.card=card.id;select.value=card.id;const breadcrumb=make('p',groupFor(card));breadcrumb.className='code-breadcrumb';content.replaceChildren(breadcrumb,make('h3',topicTitle(card)));
  content.append(make('p','Input: '+card.inputs),make('p','Output: '+card.outputs));
  content.append(make('h4',(card.id.startsWith('code-')||card.id.startsWith('solver-'))?'Repo-derived pseudocode':'Paper-level pseudocode'));
  const pre=document.createElement('pre');pre.append(make('code',card.pseudo));content.append(pre);if(card.note)content.append(make('p',card.note));
  content.append(make('h4','Implementation excerpts'));
  for(const impl of card.implementations){const detail=document.createElement('details');detail.append(make('summary',impl.symbol||impl.path));if(impl.contract)detail.append(make('p',impl.contract));if(impl.note)detail.append(make('p',impl.note));
   for(const step of impl.steps||[]){const a=make('a',impl.path+':'+step.start+'–'+step.end+' ↗');a.href=data.repo+'/blob/'+data.commit+'/'+impl.path+'#L'+step.start+'-L'+step.end;a.target='_blank';a.rel='noopener';const code=document.createElement('pre');code.append(make('code',step.code));detail.append(a,code,make('p',step.explanation))}content.append(detail)}
  const license=make('a','Source excerpts: MIT license ↗');license.href='assets/egoengine-repro-LICENSE.txt';content.append(license);
 }
 select.onchange=()=>{follow=false;render(select.value)};panel.querySelector('#code-follow').onclick=()=>{follow=true;search.value='';populate();window.updateCodePassage?.(lastBlock||document.querySelector('p.ltx_p'))};
 window.updateCodePassage=block=>{if(block)lastBlock=block;if(!follow||!block)return;let card=data.cards.find(c=>c.id===block.id);const text=block.textContent;
  if(!card){const id=/\bHPT\b/i.test(text)?'A4.SS1.p1.1':/SAM.?2/i.test(text)?'code-sam2':/MANO/i.test(text)?'code-mano':/mesh|\.obj|asset format/i.test(text)?'code-assets':/MCTS|mode.switch/i.test(text)?'S3.SS2.SSS2.p4.1':null;card=data.cards.find(c=>c.id===id)}if(card)render(card.id);
 };
 render(data.cards[0].id);try{setVisible(localStorage.getItem('egoengine-code-visible')==='true')}catch{setVisible(false)}
})();
