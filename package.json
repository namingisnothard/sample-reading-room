{
  "name": "sample-reading-room",
  "version": "1.0.0",
  "private": true,
  "description": "Local-first annotated paper reading room",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node scripts/check.mjs && node scripts/check-presenter.mjs",
    "dev": "python3 -m http.server 4174 --bind 127.0.0.1"
  }
}
